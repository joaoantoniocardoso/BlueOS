# QA Inventory — 192.168.0.124

**Collected:** 2026-08-13 (read-only probes)  
**DUT hostname:** `blueos` / mDNS `blueos124`  
**Campaign role:** second Navigator for RF wifi (after 192.168.0.177)

---

## 1. Version Chooser — `GET /version-chooser/v1.0/version/current` (200)

```json
{"repository": "bluerobotics/blueos-core", "tag": "1.4-dev", "last_modified": "2026-08-12T19:52:46.523044519Z", "sha": "sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805", "architecture": "arm"}
```

## 2. Docker — `blueos-core` (SSH)

| Field | Value |
|-------|-------|
| **RepoDigests** (container inspect) | `null` (no `RepoDigests` on running container) |
| **RepoDigests** (image inspect) | `bluerobotics/blueos-core@sha256:aa8a3f8afe37024b1ab8bf454d905002163c8673c31d1b1e214fefed4d9098ab` |
| **Image** | `sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805` |

## 3. Host Hardware (SSH)

| Probe | Result |
|-------|--------|
| `uname -a` | `Linux blueos 6.6.31+rpt-rpi-v8 #1 SMP PREEMPT Debian 1:6.6.31-1+rpt1 (2024-05-29) aarch64 GNU/Linux` |
| `/proc/device-tree/model` | `Raspberry Pi 5 Model B Rev 1.0` |
| `vcgencmd get_throttled` | `throttled=0x0` |

## 4. ArduPilot Manager

### `GET /ardupilot-manager/v1.0/board` (200)

```json
{"name": "Navigator", "manufacturer": "Blue Robotics", "platform": "navigator", "path": null, "flags": []}
```

### `GET /ardupilot-manager/v1.0/firmware_info` (200)

```json
{"version": "4.7.0", "type": "STABLE"}
```

### `GET /ardupilot-manager/v1.0/board/firmware_info` — **404**

### `GET /ardupilot-manager/v1.0/serials` (200)

```json
[{"port": "C", "endpoint": "/dev/ttyAMA0"}, {"port": "B", "endpoint": "/dev/ttyAMA2"}, {"port": "E", "endpoint": "/dev/ttyAMA3"}, {"port": "F", "endpoint": "/dev/ttyAMA4"}]
```

**MAVLink FC link:** localhost `http://127.0.0.1:6040/v1/mavlink/vehicles` → vehicle `1` present (heartbeat visible). Nginx `/mavlink2rest/v1/mavlink/vehicles` → **404** (path mismatch; use localhost or correct prefix).

## 5. Network (SSH)

### `ip -br addr`

```
lo               UNKNOWN        127.0.0.1/8 ::1/128
eth0             UP             192.168.2.2/24 192.168.0.124/24
usb0             DOWN           192.168.3.1/24
wlan0            DOWN
docker0          DOWN           172.18.0.1/16
uap0             DOWN
```

### `ip route`

```
default via 192.168.0.1 dev eth0
172.18.0.0/16 dev docker0 proto kernel scope link src 172.18.0.1 linkdown
192.168.0.0/24 dev eth0 proto kernel scope link src 192.168.0.124
192.168.2.0/24 dev eth0 proto kernel scope link src 192.168.2.2
192.168.3.0/24 dev usb0 proto kernel scope link src 192.168.3.1 linkdown
```

### WiFi radio

| Probe | Result |
|-------|--------|
| `iw dev` | ABSENT (binary not installed) |
| `nmcli -t device` | `wlan0:wifi:disconnected`, `uap0:wifi:disconnected` |
| `ls /sys/class/net` | `docker0 eth0 lo uap0 usb0 wlan0` |

**wlan0:** present, DOWN/disconnected.

## 6. WiFi Manager

### `GET /wifi-manager/v1.0/status` (200)

```json
{"bssid": null, "freq": null, "ssid": null, "id": null, "mode": null, "wifi_generation": null, "pairwise_cipher": null, "group_cipher": null, "key_mgmt": null, "wpa_state": null, "ip_address": null, "p2p_device_address": null, "address": null, "uuid": null, "ieee80211ac": null, "state": "disconnected", "disabled": null}
```

### `GET /wifi-manager/v1.0/hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/smart_hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/hotspot_extended_status` (200)

```json
{"supported": true, "enabled": false}
```

**Hotspot:** OFF (station join not blocked by hotspot).

### `GET /wifi-manager/v1.0/scan` (200)

4 networks (Cardoso, Cardoso2 on 2.4/5 GHz).

## 7. Cameras

### `/dev/video*` (SSH)

No `/dev/video0`. Pi5 ISP nodes (`video19`–`37`), `rp1-cfe` CSI at `/dev/media1` with **0 links** in media topology. **v4l2-ctl:** cannot open `/dev/video0`.

### MAVLink Camera Manager streams

| Route | Result |
|-------|--------|
| `GET http://127.0.0.1:6020/streams` (SSH localhost) | `[]` (0 active streams) |

CSI platform present; no usable V4L2 capture device / streams — **ABSENT** for USB/CSI camera journeys.

## 8. Ping / Sonar

### `GET /ping/v1.0/sensors` (200)

```json
[]
```

No Ping devices — **ABSENT**. No separate sonar service in nginx.

## 9. Serial

### `GET /bridget/v1.0/serial_ports` (200)

```json
["/dev/ttyAMA0", "/dev/ttyAMA2", "/dev/ttyAMA3", "/dev/ttyAMA4", "/dev/ttyAMA10"]
```

### `GET /bridget/v1.0/bridges` (200) → `[]`

No `/dev/ttyACM*` or `/dev/ttyUSB*` on host.

## 10. Disk

### `df -h` (SSH)

```
/dev/mmcblk0p2  117G   16G   96G  15% /
/dev/mmcblk0p1  510M   95M  416M  19% /boot/firmware
```

### `GET http://127.0.0.1:9151/disk/usage` — connection refused (service not on :9151)

## 11. Hostname / mDNS

| Probe | Result |
|-------|--------|
| `hostname` (SSH) | `blueos` |
| `GET /beacon/v1.0/hostname` (200) | `"blueos124"` |
| `GET /beacon/v1.0/vehicle_name` (200) | `"BlueROV 124 Pi5 bookworm Navigator csi-cam"` |
| `GET /beacon/v1.0/ip` (200) | `{"client_ip": "192.168.0.10", "interface_ip": "192.168.0.124"}` |

## 12. Extensions — `GET /kraken/v2.0/installed_extensions` (200)

```json
[{"identifier":"blueos.major_tom","tag":"2026-02-11","name":"major_tom","docker":"public.ecr.aws/blueos/bcloud-agent","enabled":true,"permissions":"{\"HostConfig\":{\"NetworkMode\":\"host\",\"Privileged\":true,\"Binds\":[\"/var/logs:/var/logs\",\"/:/host\", \"/var/run/docker.sock:/var/run/docker.sock\"],\"ExtraHosts\":[\"blueos:host-gateway\"]}}","user_permissions":"","auth":null}]
```

(`GET /kraken/v1.0/extensions` → **404**)

## 13. Pirate / Advanced — `GET /bag/v1.0/get/settings` (200)

```json
{"settings_version":1,"is_dark_theme":true,"is_pirate_mode":true,"is_dev_mode_enabled":false,"last_version_update_notification_time":1786489156173,"tour_version":2,"user_top_widgets":[]}
```

`/bag/v1.0/get/pirate_mode` and `/bag/v1.0/get/advanced_mode` → **400** Invalid path.

Pirate mode ON; dev/advanced (10-click) OFF.

## 14. Status

`GET /status` → **HTTP 204**

## 15. Smoke-Test Blockers

| Check | Status | Notes |
|-------|--------|-------|
| RF wifi queue | **BLOCK** | Planned second Navigator; serial RF after 192.168.0.177 |
| Hotspot | OK | `false` — does not block station join |
| wlan0 radio | OK | Present, disconnected (expected pre-RF) |
| WiFi connected / saved | **BLOCK** (mutating) | `state: disconnected`, no saved SSID in status |
| FC MAVLink heartbeat | OK | Vehicle `1` on localhost mavlink2rest |
| disk-usage API | **WARN** | :9151 not listening |
| Camera | ABSENT | CSI 0 links, no video0, 0 MCM streams |
| Ping sonar | ABSENT | Not a blocker |
| Serial bridges | **WARN** | `bridges: []` — serial-bridge fixture journeys skip |
| NMEA sockets | **WARN** | `/nmea-injector/v1.0/sockets` → **404** |
| Throttling | OK | `throttled=0x0` |
| Disk space | OK | 96G free on `/` |
