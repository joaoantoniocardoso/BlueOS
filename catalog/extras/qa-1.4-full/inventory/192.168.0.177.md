# QA Inventory — 192.168.0.177

**Collected:** 2026-08-13 (read-only probes)  
**DUT hostname:** `blueos` / mDNS `blueos177`

---

## 1. Version Chooser — `GET /version-chooser/v1.0/version/current` (200)

```json
{"repository": "bluerobotics/blueos-core", "tag": "1.4-dev", "last_modified": "2026-08-12T19:52:46.523044519Z", "sha": "sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805", "architecture": "arm"}
```

## 2. Docker — `blueos-core` (SSH)

| Field | Value |
|-------|-------|
| **RepoDigests** | `null` (local image, no registry digest) |
| **Image** | `sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805` |

## 3. Host Hardware (SSH)

| Probe | Result |
|-------|--------|
| `uname -a` | `Linux blueos 6.6.31+rpt-rpi-v8 #1 SMP PREEMPT Debian 1:6.6.31-1+rpt1 (2024-05-29) aarch64 GNU/Linux` |
| `/proc/device-tree/model` | `Raspberry Pi 4 Model B Rev 1.5` |
| `vcgencmd get_throttled` | `throttled=0x0` |

## 4. ArduPilot Manager

### `GET /ardupilot-manager/v1.0/board` (200)

```json
{"name": "Navigator", "manufacturer": "Blue Robotics", "platform": "navigator", "path": null, "flags": []}
```

### `GET /ardupilot-manager/v1.0/firmware_info` (200)

```json
{"version": "4.7.0", "type": "STABLE"}
```

### `GET /ardupilot-manager/v1.0/serial_ports` — **404** (openapi: use `/serials`)

### `GET /ardupilot-manager/v1.0/serials` (200)

```json
[{"port": "C", "endpoint": "/dev/ttyS0"}, {"port": "B", "endpoint": "/dev/ttyAMA3"}, {"port": "E", "endpoint": "/dev/ttyAMA4"}, {"port": "F", "endpoint": "/dev/ttyAMA5"}]
```

### `GET /ardupilot-manager/v1.0/preferred_router` (200)

```
"MAVLink-Server"
```

**MAVLink FC link:** mavlink2rest `/v1.0/mavlink/vehicles` returns empty — **no heartbeat / FC not visible to REST** (ABSENT from mavlink2rest).

## 5. Network (SSH)

### `ip -br addr`

```
lo               UNKNOWN        127.0.0.1/8 ::1/128
eth0             UP             192.168.2.2/24 192.168.0.177/24
usb0             DOWN           192.168.3.1/24
wlan0            DOWN
docker0          DOWN           172.18.0.1/16
uap0             DOWN
```

### `ip route`

```
default via 192.168.0.1 dev eth0
172.18.0.0/16 dev docker0 proto kernel scope link src 172.18.0.1 linkdown
192.168.0.0/24 dev eth0 proto kernel scope link src 192.168.0.177
192.168.2.0/24 dev eth0 proto kernel scope link src 192.168.2.2
192.168.3.0/24 dev usb0 proto kernel scope link src 192.168.3.1 linkdown
```

### WiFi radio

| Probe | Result |
|-------|--------|
| `iw dev` | ABSENT (binary not installed) |
| `nmcli -t device` | `wlan0:wifi:disconnected`, `uap0:wifi:disconnected` |
| `ls /sys/class/net` | `docker0 eth0 lo uap0 usb0 wlan0` |

## 6. WiFi Manager

### `GET /wifi-manager/v1.0/status` (200)

```json
{"bssid": null, "freq": null, "ssid": null, "id": null, "mode": null, "wifi_generation": null, "pairwise_cipher": null, "group_cipher": null, "key_mgmt": null, "wpa_state": null, "ip_address": null, "p2p_device_address": null, "address": null, "uuid": null, "ieee80211ac": null, "state": "disconnected", "disabled": null}
```

### `GET /wifi-manager/v1.0/hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/smart_hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/available_wifi` — **404** (openapi: use `/scan`)

### `GET /wifi-manager/v1.0/scan` (200)

4 networks visible (Cardoso, Cardoso2 on 2.4/5 GHz).

### `GET /wifi-manager/v1.0/hotspot_extended_status` (200)

```json
{"supported": true, "enabled": false}
```

## 7. Cameras

### `/dev/video*` (SSH)

18 nodes (`video0`–`video23`, `video31`). **v4l2-ctl:** 1 physical camera — `H264 USB Camera` (`/dev/video0`–`3`); remainder are Pi ISP/codec nodes.

### MAVLink Camera Manager streams

| Route | Result |
|-------|--------|
| `GET /mavlink-camera-manager/v1.0/streams` (nginx) | 200 HTML (SPA catch-all) |
| `GET http://127.0.0.1:6020/streams` (SSH localhost) | `[]` (0 active streams) |

## 8. Ping / Sonar

### `GET /ping/v1.0/sensors` (200)

```json
[]
```

No Ping devices detected — **ABSENT**.

## 9. Serial

### `GET /linux2rest/v1.0/serial` — **404** (no nginx route; service runs in-container)

### `GET /bridget/v1.0/serial_ports` (200)

```json
["/dev/ttyAMA3", "/dev/ttyAMA4", "/dev/ttyAMA5", "/dev/ttyS0"]
```

### `GET /bridget/v1.0/bridges` (200) → `[]`

## 10. Disk

### `df -h` (SSH)

```
/dev/mmcblk0p2  117G  5.3G  106G   5% /
/dev/mmcblk0p1  510M   95M  416M  19% /boot/firmware
```

### `GET /disk-usage/v1.0/disk/usage` — **404** (service not listening on :9151)

## 11. Hostname / mDNS

| Probe | Result |
|-------|--------|
| `hostname` (SSH) | `blueos` |
| `GET /beacon/v1.0/hostname` (200) | `"blueos177"` |
| `GET /beacon/v1.0/vehicle_name` (200) | `"BlueROV 177 Pi4 bookworm Navigator hd-cam"` |
| `GET /beacon/v1.0/ip` (200) | `{"client_ip": "192.168.0.10", "interface_ip": "192.168.0.177"}` |

## 12. Extensions — `GET /kraken/v2.0/installed_extensions` (200)

```json
[{"identifier":"blueos.major_tom","tag":"2026-02-11","name":"major_tom","docker":"public.ecr.aws/blueos/bcloud-agent","enabled":true,"permissions":"{\"HostConfig\":{\"NetworkMode\":\"host\",\"Privileged\":true,\"Binds\":[\"/var/logs:/var/logs\",\"/:/host\", \"/var/run/docker.sock:/var/run/docker.sock\"],\"ExtraHosts\":[\"blueos:host-gateway\"]}}","user_permissions":"","auth":null}]
```

(`GET /kraken/v2.0/extension/installed` → **405** Method Not Allowed)

## 13. Pirate / Advanced — `GET /bag/v1.0/get/settings` (200)

```json
{"settings_version":1,"is_dark_theme":true,"is_pirate_mode":true,"is_dev_mode_enabled":false,"last_version_update_notification_time":1786575116184,"tour_version":2,"user_top_widgets":["CPU","Disk","eth0 Networking"]}
```

`/bag/v1.0/get/pirate_mode` and `/bag/v1.0/get/advanced_mode` → **400** Invalid path.

Container config dirs (`docker exec blueos-core ls /root/.config`): ardupilot-manager, bag-of-holding, beacon, kraken, mavlink-camera-manager, wifi-manager, etc.

## 14. Status Page

`GET /status` → **HTTP 204**

## 15. Smoke-Test Blockers

| Check | Status | Notes |
|-------|--------|-------|
| Disk space | OK | 106G free on `/` |
| WiFi connected | **BLOCK** | `wlan0` DOWN, state `disconnected` |
| FC MAVLink heartbeat | **BLOCK** | mavlink2rest vehicles empty |
| disk-usage API | **WARN** | Service not running (:9151) |
| Camera streams | **WARN** | 1 USB cam present, 0 MCM streams configured |
| Ping sonar | ABSENT | Not a blocker |
| Throttling | OK | `throttled=0x0` |
