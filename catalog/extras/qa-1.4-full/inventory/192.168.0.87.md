# QA Inventory — 192.168.0.87

**Collected:** 2026-08-13 (read-only probes)  
**DUT hostname:** `blueos` / mDNS `blueos87`

---

## 1. Version Chooser — `GET /version-chooser/v1.0/version/current` (200)

```json
{"repository": "bluerobotics/blueos-core", "tag": "1.4-dev", "last_modified": "2026-08-12T19:52:46.523044519Z", "sha": "sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805", "architecture": "arm"}
```

## 2. Docker — `blueos-core` (SSH)

| Field | Value |
|-------|-------|
| **RepoDigests** | `bluerobotics/blueos-core@sha256:aa8a3f8afe37024b1ab8bf454d905002163c8673c31d1b1e214fefed4d9098ab` |
| **Image** | `sha256:f615d7caef4d3e99f1c068e082350c1af43d5fcc0f45379ed97ea27c6dc89805` |

## 3. Host Hardware (SSH)

| Probe | Result |
|-------|--------|
| `uname -a` | `Linux blueos 5.10.33-v7+ #1415 SMP Fri Apr 30 15:49:00 BST 2021 armv7l GNU/Linux` |
| `/proc/device-tree/model` | `Raspberry Pi 3 Model B Rev 1.2` |
| `vcgencmd get_throttled` | `throttled=0x0` |

## 4. ArduPilot Manager

### `GET /ardupilot-manager/v1.0/board` (200)

```json
{"name": "Pixhawk1", "manufacturer": "ArduPilot", "platform": "Pixhawk1", "path": "/dev/ttyACM0", "flags": []}
```

**Confirmed:** Pixhawk1 on `/dev/ttyACM0` (not Navigator).

### `GET /ardupilot-manager/v1.0/firmware_info` (200)

```json
{"version": "4.7.0", "type": "STABLE"}
```

### `GET /ardupilot-manager/v1.0/serials` — **500** (`{"detail":""}`)

### `GET /ardupilot-manager/v1.0/preferred_router` (200)

```
"MAVLinkRouter"
```

**Note:** EEPROM tooling is Pi-side (still valid on this DUT). Firmware flash targets **Pixhawk FC**, not Navigator coprocessor.

**MAVLink FC link:** mavlink2rest `GET /v1.0/mavlink/vehicles` → **404** (no REST vehicles endpoint on this build). FC presence inferred from board probe above.

## 5. Network (SSH)

### `ip -br addr`

```
lo               UNKNOWN        127.0.0.1/8
eth0             UP             192.168.0.87/24 192.168.2.2/24
wlan0            DOWN
docker0          DOWN           172.18.0.1/16
```

### WiFi radio

| Probe | Result |
|-------|--------|
| `iw dev` | ABSENT (binary not installed) |
| `nmcli radio wifi` | ABSENT (binary not installed) |
| `ls /sys/class/net` | `docker0 eth0 lo wlan0` |

## 6. WiFi Manager

### `GET /wifi-manager/v1.0/status` (200)

```json
{"bssid": null, "freq": null, "ssid": null, "id": null, "mode": null, "wifi_generation": null, "pairwise_cipher": null, "group_cipher": null, "key_mgmt": null, "wpa_state": "INACTIVE", "ip_address": null, "p2p_device_address": "ba:27:eb:0f:d6:15", "address": "b8:27:eb:0f:d6:15", "uuid": "adc05bf8-0554-5bdb-b209-4c806336af26", "ieee80211ac": null, "state": null, "disabled": null}
```

### `GET /wifi-manager/v1.0/hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/smart_hotspot` (200) → `false`

### `GET /wifi-manager/v1.0/scan` (200) → `[]` (empty)

### `GET /wifi-manager/v1.0/hotspot_extended_status` (200)

```json
{"supported": true, "enabled": false}
```

## 7. Cameras

### `/dev/video*` (SSH)

17 nodes (`video0`–`video16`). **v4l2-ctl:** 1 physical camera — `H264 USB Camera` (`/dev/video0`–`3`); remainder are Pi ISP/codec nodes.

### MAVLink Camera Manager streams

| Route | Result |
|-------|--------|
| `GET /mavlink-camera-manager/v1.0/streams` (nginx) | 200 HTML (SPA catch-all) |
| `GET http://127.0.0.1:6020/streams` (SSH localhost) | 1 active stream — `UDP Stream 0` H264 1920×1080@30 → `udp://192.168.2.1:5600`, source `/dev/video2` |

## 8. Ping / Sonar

### `GET /ping/v1.0/sensors` (200)

```json
[]
```

No Ping devices — **ABSENT**.

### Sonar service

`GET /sonar/v1.0/ping`, `/sonar/v1.0/status` → **404** (no nginx route; service **ABSENT**).

## 9. Serial

### `GET /linux2rest/v1.0/serial` — **404** (no nginx route; service runs in-container)

### `GET /bridget/v1.0/serial_ports` (200)

```json
["/dev/ttyAMA0", "/dev/ttyACM0"]
```

### `GET /bridget/v1.0/bridges` (200) → `[]`

**Pixhawk serial:** `/dev/ttyACM0` listed by bridget and ardupilot-manager board path.

## 10. Disk

### `df -h` (SSH)

```
/dev/root        15G  6.0G  7.9G  44% /
/dev/mmcblk0p1  253M   49M  205M  20% /boot
```

### `GET /disk-usage/v1.0/disk/usage` — **404**

## 11. Hostname / mDNS

| Probe | Result |
|-------|--------|
| `hostname` (SSH) | `blueos` |
| `GET /beacon/v1.0/hostname` (200) | `"blueos87"` |
| `GET /beacon/v1.0/vehicle_name` (200) | `"BlueROV 87 Pi3 bullseye Pixhawk hd-cam"` |

## 12. Extensions — `GET /kraken/v2.0/installed_extensions` (200)

```json
[{"identifier":"blueos.major_tom","tag":"2026-02-11","name":"major_tom","docker":"public.ecr.aws/blueos/bcloud-agent","enabled":true,"permissions":"{\"HostConfig\":{\"NetworkMode\":\"host\",\"Privileged\":true,\"Binds\":[\"/var/logs:/var/logs\",\"/:/host\", \"/var/run/docker.sock:/var/run/docker.sock\"],\"ExtraHosts\":[\"blueos:host-gateway\"]}}","user_permissions":"","auth":null}]
```

(`GET /kraken/v1.0/installed_extensions` also 200, same payload)

## 13. Pirate / Advanced — Bag of Holding

`GET /bag/v1.0/get/settings` → **400** Invalid path (no `settings` key on this DUT).

Container file `/root/.config/bag-of-holding`:

```json
{"vehicle.image_path": {"url": "/assets/vehicles/images/bluerov2.png"}, "wizard": {"version": 4}}
```

`GET /bag/v1.0/get/wizard` (200) → `{"version":4}`. Keys `is_pirate_mode`, `advanced_mode`, `settings` → **400** Invalid path.

**Pirate / advanced:** **ABSENT** from bag (not stored; likely default off).

## 14. Status Page

`GET /status` → **HTTP 204**

## 15. Smoke-Test Blockers

| Check | Status | Notes |
|-------|--------|-------|
| Disk space | OK | 7.9G free on `/` |
| WiFi connected | **BLOCK** | `wlan0` DOWN, `wpa_state` INACTIVE, scan empty |
| FC platform | **NOTE** | Pixhawk1 `/dev/ttyACM0`; flash/EEPROM smoke must target FC, not Navigator |
| ardupilot serials API | **WARN** | `/serials` returns 500 |
| mavlink2rest vehicles | **WARN** | `/v1.0/mavlink/vehicles` 404 on this build |
| disk-usage API | **WARN** | Service not listening (:9151) |
| Camera streams | OK | 1 USB cam, 1 MCM stream running |
| Ping / sonar | ABSENT | Not a blocker |
| Throttling | OK | `throttled=0x0` |
| Pirate mode | ABSENT | Not in bag; destructive smoke may need enabling first |
