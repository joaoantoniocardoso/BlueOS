# P3d: Availability modeling

Decision D3: FeatureAvailability (now Availability) is modeled as modeling metadata (version presence), NOT SysML variation/variant, until the P5 exporter exists. Variability is the semantically right SysML mapping; metadata is the cheap reverse-engineering encoding. Revisit in P5.
