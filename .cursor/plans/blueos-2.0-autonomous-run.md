# BlueOS 2.0 Catalog — Autonomous Orchestrator Run Book

> **This file is the single source of truth for the autonomous run.** It is written so that a
> *fresh* instance of the orchestrator (after a crash / context loss) can read this file + `git log`
> and resume EXACTLY where the previous instance stopped, without re-asking the user.
> **Update this file (the ledger + decisions log) as the FIRST action after each artifact is accepted
> and committed.** Never let it go stale.

## Mission

Model every BlueOS core service into the `blueos-catalog` Rust crate across its four layers
(**Observed → Journeys → Asserted → Runtime**), each independently QA-gated, then harden the harness.
The catalog is the machine-verifiable single source of truth for the BlueOS 2.0 service rework.

## Hard guardrails (DO NOT GO ROGUE)

1. **Only model services that exist** in `core/start-blueos-core` (the 26 in the ledger). Do not invent services, routes, or capabilities.
2. **Never fabricate a fact.** Every observed fact cites `file:line`; every asserted value has a rationale; every runtime value traces to a live-capture artifact. No evidence → `Unknown{reason}`.
3. **Runtime values come ONLY from the live BlueOS Pi** at `192.168.0.177` (user `pi`, pass `raspberry`, container `blueos-core`). **Floating tag `master` is not a pin** — always re-read `GET /version-chooser/v1.0/version/current` (or `docker inspect` RepoDigest) and record `bluerobotics/blueos-core:<tag> @ sha256:<digest>` in every `environment` string (ideally with `captured_at`). Historical baseline: `bluerobotics/blueos-core:master @ sha256:cdccc74464076e7fa8b5dc8a85c83db0ec95c27cb77130cb1e180d481320674e`; the play Pi may have drifted since. The POC `../microservices_core_prototype` is design-reference only, never a value source. If the Pi is unreachable, mark runtime `Unknown` and move on — do NOT block.
4. **Follow the harness**: dispatch composer-2.5 subagents per role; the orchestrator QAs (independent, fresh subagent) and adjudicates but does not author artifacts it will QA. Every artifact passes `cargo fmt` + `clippy -D warnings` + `test` + `drift` + `Catalog::validate()` before acceptance.
5. **One service per unit of work.** Keep the crate green at every commit. Commit after each service (or each harness fix) with the repo commit-style (`catalog: ...` / path-prefixed, capitalized).
6. **Mutating runtime captures must be reversible** and the vehicle restored to pre-capture state. Do not run destructive ops (firmware bricking, factory reset) without a restore path.
7. **Do not touch git config, force-push, or amend pushed commits.** Never modify files outside this repo except reading `../BlueOS-docs` and the live Pi.
8. **When uncertain, prefer `Unknown{reason}` over a guess.** Truthfulness beats coverage.
9. **Journey/runtime live-URL gate.** Before committing any change to `catalog/src/journeys/**`, journey `RouteRef`s, runtime capture keys used by journey outcomes, or `runner`/`validate` route resolution: run `BLUEOS_BASE=http://192.168.0.177 bash catalog/gate.sh` (or at minimum `cargo run -q --bin journey_http -- --base http://192.168.0.177 --smoke --fixtures internet,pirate,advanced`). Require **`failed=0`**. Optional deeper check: `journey_http --mutating-smoke` exercises the allowlisted reversible mutating journeys only (theme change/reset, LAN speed POST, branding DELETEs — see `MUTATING_SMOKE_JOURNEY_IDS`); it is **not** required for RouteRef commits. If the Pi is unreachable: do not invent routes; either leave outcomes `Unknown` / mark steps `UnverifiedLive` in the QA report and ledger, or defer the commit — **never** commit `Known` `expected_status` against an unverified `RouteRef`.

## Resume protocol (run this on every fresh start)

1. Read this file top to bottom.
2. Run `git -C . log --oneline -15` and `git status` to see what is committed vs in-flight.
3. Find the **first ledger row / phase step that is not `DONE`** and continue from there.
4. If an artifact is half-written (uncommitted), run the gates; fix or finish it; QA; commit; update ledger.
5. Never restart completed work. Never ask the user — proceed autonomously per the guardrails.

## Harness recipe (condensed — full detail in `blueos-2.0-service-catalog.md`)

Model slug for all subagents: **composer-2.5**. Roles + skills:
- **Fact Extractor** → `.cursor/skills/blueos-service-extraction/SKILL.md` → `observed_facts()` in `catalog/src/services/<id>.rs`
- **Docs Specialist** → `.cursor/skills/blueos-journey-extraction/SKILL.md` → `catalog/src/journeys/<id>.rs` (doc-grounded)
- **Card Author** → `.cursor/skills/blueos-service-card/SKILL.md` → `service_definition()` in `catalog/src/services/<id>.rs`
- **Runtime Specialist** → `.cursor/skills/blueos-runtime-capture/SKILL.md` → `runtime_facts()` + journey outcomes, from a live-Pi capture artifact under `catalog/runtime-captures/<id>__pi4_*.json`
- **QA Reviewer** (fresh, per artifact) → `.cursor/skills/blueos-catalog-validate/SKILL.md` → ACCEPT/BOUNCE

**Per-service order** (keeps crate green; wire mod.rs incrementally):
1. Recon (orchestrator): nginx route, start-blueos-core tuple, service dir, docs section.
2. Fact Extractor → observed → wire `all_observed()` → QA → fix bounces → commit-ready.
3. Docs Specialist → journeys (declare `mod`, defer `all_journeys()` wiring) → QA. Route hints MUST be smoke-ready (nginx version + prefix + path + query); when Pi reachable, `journey_http --smoke` MUST pass (`failed=0`) before commit.
4. Card Author → `service_definition()` → wire `all_service_definitions()` + `all_journeys()` (journeys need service+capabilities to validate) → QA.
5. Runtime Specialist → `runtime_facts()` → wire `all_runtime()` → QA. Re-grounded journey outcomes MUST pass smoke (`failed=0`) when Pi reachable; otherwise leave `Unknown` / `UnverifiedLive` — never commit `Known` status against an unverified route. (Skip/Unknown if Pi unreachable or low value.)
6. Orchestrator: full gates, update ledger, commit.

Templates to copy: `catalog/src/services/ardupilot_manager.rs`, `catalog/src/services/kraken.rs`, `catalog/src/journeys/{ardupilot_manager,kraken}.rs`.
Reusable capture tools: `catalog/runtime-captures/tools/{sample_resource.sh,probe_http.sh}` (params documented in their headers).

## Reusable capture facts
- Pi: `192.168.0.177`, ssh `pi:raspberry`, container `blueos-core`, board Navigator. **Re-read `/version-chooser/v1.0/version/current` before capture** — do not assume `master`; identity is digest. Historical baseline: `bluerobotics/blueos-core:master @ sha256:cdccc74464076e7fa8b5dc8a85c83db0ec95c27cb77130cb1e180d481320674e`.
- Resource sample: `bash tools/sample_resource.sh --match "<id>/main.py" --samples 60 --label <state> --out <file>`
- HTTP SLO probe: `bash tools/probe_http.sh --base http://192.168.0.177/<prefix>/v2.0 --gets "/a /b" --repeats 40 --out <file>`
- Get routes: `curl -s http://192.168.0.177/<prefix>/v{ver}/openapi.json | jq ...`

---

## PHASE CHECKLIST

- [x] **Phase 1 — Finish kraken**: DONE. Captured all 4 gaps live (custom install POST /extension/ 200; add-manifest POST /manifest/ 201 + DELETE 204; tag edit PUT /extension/{id}/{tag} 200; container log GET .../log 200). All 10 kraken journey route-steps now runtime-grounded; no Unknown outcomes remain. Vehicle restored.
- [x] **Phase 2 — Fix harness**: DONE. (2) `extract` binary — parses `core/start-blueos-core`, emits `ExtractedService` (`--json`), and self-checks the observed layer vs source (test `bootstrap_observed_matches_source`). (3) `drift` — real asserted↔observed (resource-evidence) + asserted↔runtime (state-machine/state existence); `diff_catalog_runtime` wired into the bin. (4) inter-rater rubric FROZEN v1.0 in `blueos-service-card/SKILL.md` — calibration 13/14 (93%) hard-field agreement; codified 4 decision rules (user_confirmation⇐dangerous_ops non-empty; dangerous_ops = irreversible/untrusted-code/unsafe-state only; authorities = exclusive rights others depend on; bounded_context = semantic). No catalog data changes needed — committed cards already comply.
- [x] **Phase 3 — Harness eval & polish**: DONE. Audited validator/skills/rules/bins. Fixes: added `catalog/gate.sh` (one-shot fmt+clippy+test+extract+drift+export, fail-fast) as the canonical per-slice/resume gate; QA skill now runs `extract` and enforces FROZEN RUBRIC v1.0 on judgment fields. Verified: no stale type/IP refs; validator covers authority uniqueness, edge/journey/runtime cross-refs, and state existence. See "PHASE 3 AUDIT" section for loose ends + deliberate deferrals.
- [x] **Phase 4 — The hard work**: DONE. (7) Orchestrated ALL remaining services one by one via the composer-2.5 subagent harness with independent QA. 100% of the start-blueos-core roster (26 distinct services) is now modeled across all four layers (observed + journeys + asserted + runtime), every `bash gate.sh` GREEN, committed one-per-service with post-commit ledger updates. New harness precedents: Bus::File (recorder→recorder_extractor) and Bus::Subprocess (ttyd→user_terminal) cross-service edges, plus ObservedFacts kind Shell. See ledger below + DECISIONS LOG.

---

## SERVICE LEDGER (Phase 4 + calibration)

Status values: `TODO` / `WIP` / `DONE`. Columns: Observed / Journeys / Card / Runtime. Runtime `n/a`
allowed for services with no meaningful runtime or unreachable. Update after each commit.

| # | Service (tmux) | catalog id | port/prefix | kind | Obs | Jrn | Card | Rt | Committed |
|---|---|---|---|---|---|---|---|---|---|
| 1 | autopilot | ardupilot_manager | 8000 /ardupilot-manager/ | python | DONE | DONE | DONE | DONE | ad5950b93 |
| 2 | kraken | kraken | 9134 /kraken/ | python | DONE | DONE | DONE | DONE | e5ffdaf8a (+Phase1) |
| 8 | beacon | beacon | 9111 /beacon/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; mDNS = harness gap (no Interface variant) |
| 9 | cable_guy | cable_guy | 9090 /cable-guy/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; netlink/D-Bus mutation = harness gap |
| 10 | wifi | wifi | 9000 /wifi-manager/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; wpa ctrl-socket/D-Bus = harness gap |
| 11 | versionchooser | versionchooser | 8081 /version-chooser/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; core-image updater; dangerous ops = Upgrade/delete/pull |
| 12 | bag_of_holding | bag_of_holding | 9101 /bag/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; JSON store; 1 journey (Bag Editor), rest is infra |
| 13 | customization | customization | 9152 /customization/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; Auxiliary branding; no dangerous ops |
| 14 | nmea_injector | nmea_injector | 2748 /nmea-injector/ | python | DONE | DONE | DONE | DONE | FULLY MODELED; ext-GPS→GPS_INPUT via m2r; dynamic listener = gap |
| 15 | pardal | pardal | 9120 /network-test/ | python(aiohttp) | DONE | DONE | DONE | DONE | FULLY MODELED; unversioned; LAN throughput + WAN speedtest; measurement-only |
| 16 | ping | ping | 9110 /ping/ | python(v1.0) | DONE | DONE | DONE | DONE | FULLY MODELED; FIRST non-root py svc (run_as blueos → PrivilegeLevel::Regular); BR sonar mgr; spawns `bridges` binary; dynamic UDP = gap |
| 17 | bridget | bridget | 27353 /bridget/ | python(v1.0) | DONE | DONE | DONE | DONE | FULLY MODELED; non-root (Regular); user-configured serial↔UDP bridges; spawns `bridges` binary; OutboundHttp→linux2rest:6030; dynamic UDP = gap |
| 18 | recorder_extractor | recorder-extractor | 9150 /recorder-extractor/ | python(v1.0) | DONE | DONE | DONE | DONE | FULLY MODELED; root; MCAP→MP4 extract + video gallery; 5 subprocs (mcap/gst); DELETE=dangerous(delete_recording); shares recorder dir w/ recorder svc |
| 19 | mavlink2rest | mavlink2rest | 6040 /mavlink2rest/ | **BINARY** (rust) | DONE | DONE | DONE | DONE | FULLY MODELED; FIRST BINARY + FIRST CONCRETE EDGE (→ardupilot_manager via paired udpout/udpin:14001); Priority tier; MAVLink↔REST/WS bridge; tier Important; dangerous_ops=[] (transparent relay) |
| 20 | linux2rest | linux2rest | 6030 /system-information/ | **BINARY** (rust) | DONE | DONE | DONE | DONE | FULLY MODELED; read-only host system-info REST; unversioned; tier Important (bridget/helper depend); dangerous_ops=[]; authorities system_information_provider; edges=[] |
| 21 | video | mavlink-camera-manager | 6020 /mavlink-camera-manager/ (+/webrtc/ws/ :6021) | **BINARY** (rust) | DONE | DONE | DONE | DONE | FULLY MODELED; tmux `video` ≠ id `mavlink-camera-manager`; PRIORITY tier; 4 journeys; tier Important; dangerous_ops=[] (stream removal reversible); authorities camera_stream_manager; **2nd concrete edge → ardupilot_manager via tcpout/tcpin:5777**; runtime: no cameras attached (v4l=ISP nodes, streams=[]) |
| 22 | zenohd | zenohd | 7117 /zenoh/ (REST) + 7118 /zenoh-api/ (ws) | **BINARY** (rust) | DONE | DONE | DONE | DONE | FULLY MODELED; Zenoh pub/sub message-bus router; Normal tier; 1 journey (Zenoh Inspector, Advanced vis); tier Important (flight control not via zenoh); dangerous_ops=[]; authorities ZenohBroker (first-class enum); edges=[] (hub, inbound only); runtime bursty CPU (med 4%/p95 59%), adminspace GETs 200 |
| 23 | nginx | nginx | :80 / (+2770 redirect, /cache/ /upload/ /userdata/ /status) | **BINARY** (C) | DONE | DONE | DONE | DONE | FULLY MODELED; HTTP reverse-proxy front door; Normal tier mem 250; 1 journey (access_blueos_web_interface, doc-grounded vis); tier Important (web access depends, flight control via MAVLink does not); **dangerous_ops=webdav_file_mutation (/upload DAV DELETE/MOVE) user_confirmation Required**; authorities NginxProxy (first-class enum); edges=[] (ingress hub); QA bounced health overclaim (/status = unconditional 204 liveness) → fixed; runtime multi-proc master idle+5 workers ~22MB, routes sub-ms |
| 24 | ttyd | ttyd | 8088 /terminal/ (websocket) | **BINARY** (C) | DONE | DONE | DONE | DONE | FULLY MODELED; web terminal (root shell in browser); Normal tier mem 250; Websocket + Subprocess(tmux user_terminal); 1 journey (access_web_terminal, Advanced vis); tier Auxiliary (dev convenience, danger≠tier); **dangerous_ops=arbitrary_root_shell_execution user_confirmation Required (highest-impact cap in catalog)**; authorities web_terminal; edges=Unknown (deferred user_terminal tmux dep); runtime idle ~1.3MB, /terminal/ 200 (ws NOT opened for safety) |
| 25 | iperf3 | iperf3 | 5201 (raw iperf TCP, no HTTP) | **BINARY** (C) | DONE | DONE(empty) | DONE | DONE | FULLY MODELED; raw bandwidth server; Normal tier mem 250; empty interfaces (no HTTP variant fits) + listen 5201; ZERO journeys (undocumented machine-only, bandwidth docs point to pardal); tier Auxiliary; dangerous_ops=[]; authorities=[] (non-exclusive like pardal); edges=[]; runtime idle ~1.3MB, :5201 listener open (no throughput test run) |
| 26 | filebrowser | filebrowser | 7777 /file-browser/ | **BINARY** (Go) | DONE | DONE | DONE | DONE | FULLY MODELED; web file manager; Normal tier mem 250 nice -19; Rest :7777 + db /etc/filebrowser/filebrowser.db; 1 journey (manage_blueos_files, Advanced vis, embedded SPA route:None); tier Auxiliary; **dangerous_ops=arbitrary_file_mutation user_confirmation Required (edit/delete/move root files)**; authorities file_browser; edges=[]; runtime idle ~15.9MB, /file-browser/ 200 + /health 200 ("status":"OK"), /api auth-gated 401 |
| 27 | recorder | recorder | (none, no HTTP) | **BINARY** (Rust ext.) | DONE | DONE(empty) | DONE | DONE | FULLY MODELED; blueos-recorder background daemon; Normal tier mem 250; NO nginx route, empty interfaces (no citable zenoh flag) + resource /usr/blueos/userdata/recorder SharedWrite; ZERO journeys (background daemon, UI is recorder_extractor); tier Auxiliary; dangerous_ops=[] (additive MCAP writes); authorities session_recorder; **edges recorder->recorder_extractor via Bus::File (required_at_boot=false, first filesystem edge)**; runtime PID 1272 ~9.4MB RSS ~6% idle CPU, recording dir empty (idle); binary reports v0.1.0 (bootstrap says 0.0.5) |
| 28 | user_terminal | user_terminal | (none, no HTTP) | **SHELL** (tmux) | DONE | DONE(empty) | DONE | DONE | FULLY MODELED (LAST); interactive root-shell tmux session; Normal tier mem 0 cpu 0; entrypoint `cat /etc/motd`; kind Shell; empty interfaces/resources; ZERO journeys (web-terminal journey lives on ttyd); tier Auxiliary; 1 cap provide_interactive_root_shell; dangerous_ops=[] (root-shell hazard surfaced via ttyd, not double-counted); authorities=[]; edges=[] leaf; **RESOLVED ttyd->user_terminal edge via Bus::Subprocess (required_at_boot=false, self-heals via `tmux attach||tmux new`)**; runtime session alive since boot, idle bash PID 1040 ~2.5MB ~0% CPU |
| 10 | commander | commander | 9100 /commander/ | python | DONE | DONE | DONE | DONE | FULLY MODELED (RSS~35MB; read-only SLO; dangerous POSTs Unknown by design) |
| 12 | helper | helper | 81 /helper/ | python | DONE | DONE | DONE | DONE | FULLY MODELED (RSS~62MB, cpu~1.06%; v1.0 GETs) |
| 25 | disk_usage | disk_usage | 9151 /disk-usage/ | python | DONE | DONE | DONE | DONE | FULLY MODELED (RSS~35MB, cpu~0.29%; du/ heavy) |

> LEDGER NOTE (2026-07-11): all stale duplicate `TODO` rows from the initial draft were removed after
> verifying each service is genuinely 4-layer DONE (observed+journeys+asserted+runtime, wired into
> mod.rs). The table now lists every one of the 26 distinct services exactly once, all DONE. Row `#`
> values are historical draft ids (not contiguous) — the authoritative completeness signal is the
> per-service 4-layer wiring in `catalog/src/services/mod.rs`, cross-checked green by `bash gate.sh`.

---

## >>> RESUME POINTER (update every service) <<<
- Phases 1-3: DONE. Harness is built + hardened (gate.sh, extract, drift, frozen rubric).
- Phase 4 progress: **COMPLETE — ALL 26 catalog services FULLY MODELED** (18 py + 8 binaries + 1 external Rust binary + 1 shell session; where "26" counts distinct services incl. ardupilot_manager[=autopilot tmux] and mavlink_camera_manager[=video tmux]). Roster cross-check vs start-blueos-core PRIORITY_SERVICES+SERVICES: 100% coverage, no service left. Binaries/shell: mavlink2rest, linux2rest, mavlink-camera-manager, zenohd, nginx, ttyd, iperf3, filebrowser, recorder (external), user_terminal (shell). Every service: observed + journeys(may be empty) + asserted + runtime, all `bash gate.sh` GREEN, committed one-per-service with ledger updated post-commit.
- IMAGE NOTE (CORRECTED): the live Pi image did NOT change. sha256:cdccc744 = the image CONFIG ID (`docker inspect .Image`); sha256:0406983a = the same image's RepoDigest (registry `@sha256:` manifest). Both identify ONE image. The linux2rest artifact recorded the RepoDigest, all others the config ID — same image throughout. Earlier "image advanced" note was wrong (config-id vs repo-digest confusion), now retracted.
- BINARY PATTERN ESTABLISHED (mavlink2rest, ACCEPTED first-pass): kind=Binary; anchor deployment in start-blueos-core tuple + nginx.conf; id==tmux name (so extract gate cross-checks tier/mem/cpu); zenoh_log_topic/sentry/logs_path = Unknown (rust binary, no python init_logger); versions=[] (don't fabricate); MavlinkRole out→Consumer, in→Endpoint (interface.rs:51-52); sample_resource.sh now supports `--pid` for binaries. Edges CAN be asserted concretely when both sides' observed interfaces pair up (verify BOTH sides in source).
- **Phase 4 (four-layer modeling of all 26 services) is COMPLETE.** Revisiting the MASTER plan (`blueos-2.0-service-catalog.md`) surfaced two milestone gaps the run book never tracked: the **M2 edge graph** and **M3 clustering**.
- **M2 EDGE GRAPH: DONE.** Edges-pass resolved every remaining `edges: unknown` now that all targets are cataloged. Concrete Rest edges (helper→{versionchooser,mavlink2rest}, bridget→linux2rest, ping→mavlink2rest, nmea_injector→mavlink2rest) + ardupilot_manager→zenohd (Zenoh MAVLink bridge :7117); 10 external/self/hardware-only services set to determined-empty `established(vec![])`. Zero `edges: unknown` remain. Two commits, both QA-ACCEPTed, gate green.
- **M3 CLUSTERING: DONE.** Real `coupling_matrix(policy)` + `src/cluster.rs`/`src/bin/cluster.rs`: per-bus×failure weights (WEIGHTS_VERSION v1) + shared-resource + policy affinity; CNM modularity clustering (deterministic); `cluster_stability()`; `boundary_proposals()`→3. QA ACCEPT, 33 tests, no new deps, reproducible. CouplingOnly Q≈0.245.
- **F2 FRONTEND LAYER: DONE.** Architect surfaced the biggest blind spot: BlueOS 1.x implements whole journeys IN the frontend and holds domain state in the browser — invisible to the backend-only model. Added the `Page` artifact (`src/page.rs`), the **Frontend Extractor** agent, froze the rubric on 3 calibration pages, then **modeled all 24 pages** (4 Extractor batches + independent per-batch QA; `PageId` enum expanded to 24; `PAGES` slice). QA caught 6 pages mislabeling backend-owned capabilities as `frontend_features` → orchestrator adjudicated against the registry (frontend-origin = not in `CAPABILITIES`), fixed, and hardened the rule with a mechanical registry test. **Merged frontend features into the inventory**: typed `Feature.origin = Origin::{BackendService(ServiceId), FrontendPage(PageId)}`, 14 frontend-origin capabilities registered in `FRONTEND_CAPABILITIES` → 143-feature inventory (129 backend + 14 frontend); `FeatureCatalog::validate()` enforces the split. Two commits (`552f2816a` pages, `44c8ce757` origin), gate GREEN. Also completed the **typed const-catalog refactor** (ID enums, `&'static` data, `Service` aggregate, capability registry, `Entity` trait).
- **F3 FRONTEND JOURNEYS: DONE.** Closed the journey-layer blind spot: the 86 journeys were all backend/operator flows; the browser-implemented wizards (calibration, param-file apply, video-stream config) existed only as `Page.frontend_features`, never as first-class `UserJourney`s. Added `Actor::Frontend(PageId)` (zero churn — additive to the `Actor` enum), extended `validate()` to accept frontend-origin `capability_refs` (registered in `FRONTEND_CAPABILITIES`), and authored **8 new journeys** (6 calibration + 1 parameter-apply + 1 video-stream) covering all 14 frontend-origin capabilities. Each step triangulated Doc (`../BlueOS-docs/content/usage/advanced/index.md`) + Source (exact Vue `file:line`), mutating wizard outcomes left `Grounded::unknown` (no fabrication). Independent QA audited all 64 anchors (63 PASS, 1 BOUNCE fixed: accel status re-derive re-grounded from `calibration.ts:33` → `store/ardupilot_sensors.ts:40`). 94 journeys total, gate GREEN (47 lib tests, clippy clean). Rule + skill updated with the frontend-journey convention.
- **M4 — HUMAN (F2/F3 complete; this is the remaining step).** Architecture-decision workshop (score the 3 clustering proposals, fold the 19 aggregates / 7 workflow communities + re-home `FrontendOwned` state — now catalog-queryable per page — into candidate 2.0 bounded contexts, event-storm, ADRs, strangler slices) — explicitly a human step. Do NOT invent boundary decisions.
- BINARY launch tuples (start-blueos-core): video/mcm L120 (:6020 /mavlink-camera-manager/), mavlink2rest L121 DONE, zenohd L128 (/zenoh/,/zenoh-api/), iperf3 L135 (:5201 server, mem250), linux2rest L136 (:6030 /system-information/, mem250), filebrowser L137 (/file-browser/, --baseurl, mem250), ttyd L142 (:8088 /terminal/, tmux attach user_terminal, mem250), nginx L143 (the proxy itself, mem250, nice-18), recorder L145 (blueos-recorder --recorder-path /usr/blueos/userdata/recorder, mem250). nginx prefixes: mcm=/mavlink-camera-manager/→6020, linux2rest=/system-information/→6030, ttyd=/terminal/→8088, filebrowser=/file-browser/, zenoh=/zenoh/ + /zenoh-api/. (mavlink2rest, linux2rest, video, zenohd, nginx, ttyd, iperf3, filebrowser, recorder) then user_terminal. NOTE: binaries are NOT python services — no main.py/observed-from-python; they are external binaries launched by start-blueos-core. Extraction anchors come from start-blueos-core (entrypoint/tier/port), nginx.conf (prefix), and the binary's upstream repo/docs. ServiceKind will be a non-python variant. Reassess the observed rubric per-binary.
- Per-service loop (each layer committed separately, ledger updated): Fact Extractor(self-recon)→QA(observed)→Docs Specialist(journeys)→Card Author(wires all_journeys + service_def)→QA(card+journeys)→Runtime Specialist(live Pi)→commit. Run `bash catalog/gate.sh` before every commit. Pi at 192.168.0.177 (pi:raspberry). NEVER call destructive endpoints during capture.

## DECISIONS LOG (append-only; newest last)

- 2026-07-19: **RUNTIME VERSION-PIN POLICY.** Floating tag `master` is a channel label, not a capture identity. Every `Provenance::Runtime.environment` MUST include `bluerobotics/blueos-core:<tag> @ sha256:<digest>` (ideally `captured_at`); capture filenames may keep `__master` as the channel suffix. Before claiming an environment, re-read `GET /version-chooser/v1.0/version/current` on the play Pi (`192.168.0.177`) — hardware may drift. Historical baseline digest: `sha256:cdccc74464076e7fa8b5dc8a85c83db0ec95c27cb77130cb1e180d481320674e`. Documented in service-catalog plan, tier2 restore toolkit (tag vs digest for core switch), runtime-capture skill, and guardrail #3 here.

- 2026-07-19: **TIER-2 CORE SWITCH + MDNS + WIFI HARNESS PLAN.** (1) `switch_local_blueos_version` undefferred: `docker tag` master→`smoke-catalog-switch` (same image bits), POST `/version/current`, `wait_for_blueos`, restore to master. Versionchooser kills core before HTTP 200 — runner accepts HTTP 0/transport fail then verifies tag. (2) `change_mdns_hostname` undefferred: POST `smoke-catalog`, restore `blueos`. (3) `connect_to_wifi_network` stays deferred. Final CI topology: Pi3 tester/harness ↔ Pi4 DUT — Pi3 hotspot for DUT join; Pi3 joins BlueOS emergency hotspot. Documented in `.cursor/plans/blueos-2.0-tier2-restore-toolkit.md`.

- 2026-07-18: **TIER-2 FIRMWARE + EXTENSIONS SAFE ON PLAY PI.** Operator policy: firmware flash (URL/file/restore/default/first-boot) and extension install/uninstall/edit/restart/disable are reversible Tier-2 targets — not deferred. Runner undeffers those journeys; binds Example1 / major_tom / alpine; ships `catalog/fixtures/smoke-firmware.bin`. Live: `passed=77 failed=0 skipped=8`. Still deferred: core/bootstrap image switch, host reboot, mdns hostname.

- 2026-07-18: **TIER-2 SETUP PROVISIONING (HttpRoundTrip).** Cleared 4 live mutating-smoke skips via setup/teardown: NMEA sock create→delete, serial bridge create→delete (`/dev/ttyAMA3`), 3D model upload→DELETE with path bind + grounded 204, DHCP enable→disable. Cable_guy restore: delete smoke static `192.168.0.178`, `DELETE /dhcp` after enable/disable, restore host DNS. Default fixtures add `nmea-socket,serial-bridge,dhcp-active`. Live: `passed=55 failed=0 skipped=17` (was 21). Note: cable_guy `settings-2.json` can still accumulate junk routes (e.g. gateway `192.168.0.1`) — filesystem replace teardown still TODO.

- 2026-07-18: **TIER-2 SETUP = REPAIR TOOLKIT.** Preconditions for mutating-smoke are not merely “fixture present → run / missing → skip.” The same play-Pi repair mechanisms used for restore (HTTP, filesystem, tmux, container/host restart, arbitrary commands, caching proxy) may **provision** preconditions before mutate (e.g. seed `Data::ExtensionInstalled`, enable pirate mode, create an NMEA sock, pull a local BlueOS tag). `FixtureInventory` still declares what the run provides; the runner may create that state. Skip only when setup is impossible or deliberately out of scope. Plan: `.cursor/plans/blueos-2.0-tier2-restore-toolkit.md`.

- 2026-07-18: **TIER-2 RESTORE TOOLKIT (play Pi).** Live Pi at `192.168.0.177` is owned playground hardware (reimage OK). Tier-2 policy is **reversibility**, not “never mutate.” Allowed restore mechanisms for mutating-smoke teardown: (1) HTTP snapshot/restore and create/destroy pairs; (2) download/modify/re-upload files in the `blueos-core` container or on the Pi host; (3) restart a service via its tmux session (Ctrl-C, Up, Enter); (4) restart the `blueos-core` container or reboot the Pi and wait for BlueOS; (5) run arbitrary commands on container and host as needed; (6) optional caching proxy for external deps (Docker Hub, etc.) so pulls are reproducible offline. **Hard exclude from automated Tier-2:** `shutdown` (and any power-off with no wake path). Reboot is allowed if the runner waits for recovery. Bag `/overwrite`, settings reset, firmware, version switch, wifi/IP, EEPROM, host commands are allowlist-eligible **only** when a concrete restore plan is declared for that entry. Supersedes the v2 allowlist note’s blanket “excludes destructive flows” wording.

- 2026-07-18: **TIER-1 GET COVERAGE 100%.** Every concrete GET `RouteRef` on `Automatable::Http` journeys has `expected_status` (43/43). Gate: `tier1_get_coverage` bin + test `tier1_get_coverage_is_complete`. Wildcards (`*` / `{…}`) are templated (out of gate). Long-running SSE/WAN GETs (`/disk/speed/stream`, `/internet_download_speed`, `/internet_upload_speed`) are asserted but smoke-excluded. Live `--smoke` @ Pi: `passed=26 failed=0`. Commit `3cd74f81f`.

- 2026-07-18: **TIER-3 FRONTEND PAGE-LOAD SMOKE.** Playwright under `catalog/e2e/` exercises `Actor::Frontend` entry points for the v1 calibration page set (`vehicle_setup`, `video_manager`, `parameter_editor`). `cargo run --bin frontend_smoke` derives concrete paths from catalog `Page.route` via `concrete_page_path` (strips Vue `:param` segments); Playwright loads `pages.json` and asserts `#app` + HTTP &lt;400. Full wizard automation deferred (needs vehicle/MAVLink). **Policy:** optional live check — NOT wired into `gate.sh` (unlike Tier-1 GET smoke). Plan: `.cursor/plans/blueos-2.0-tier3-frontend-smoke.md`. Deep links use SPA workaround (load `/` then Vue Router push) because nginx has no index.html fallback.

- 2026-07-18: **TIER-2 MUTATING SMOKE ALLOWLIST.** `journey_http --mutating-smoke` exercises only reversible, non-destructive mutating HTTP on a live Pi. v1 allowlist: `change_ui_theme_color` (PUT `/customization/v1.0/theme` with JSON `{"primary":"#1e88e5"}`) + `reset_ui_theme_color` (DELETE, restores defaults). Runner filters non-GET steps with `expected_status`, skips templated paths, and enables mutating only for allowlisted journeys — no blanket `--allow-mutating`. Mutually exclusive with `--smoke`. **Policy:** Tier-1 GET smoke remains the blocking RouteRef commit gate; mutating-smoke is an optional deeper check, not required for journey/route commits.

- 2026-07-18: **TIER-2 MUTATING SMOKE ALLOWLIST v2.** Expanded allowlist (order preserved): `change_ui_theme_color`, `reset_ui_theme_color`, `run_lan_speed_test` (POST `/pardal/post_file` → 200, empty body OK), `remove_custom_logo` (DELETE `/customization/v1.0/branding/logo` → 204, idempotent), `remove_custom_vehicle_image` (DELETE `/customization/v1.0/branding/vehicle-image` → 204, idempotent). Still excludes destructive flows (bag overwrite, firmware, reboot, wifi connect, multipart uploads). Live `--mutating-smoke` @ Pi: expect `passed=5 failed=0` (one mutating step per journey).

- 2026-07-18: **DOMAIN WBS ENCODED (M4 input).** Subject-axis hierarchy typed as `Domain` → `Aggregate` → `Feature`: 6 domains (`vehicle`, `peripherals`, `onboard_computer`, `network`, `blueos_platform`, `presentation`), 20 aggregates (split `PlatformInfra` → `WebIngress` + `MessageBus`), 143 features unchanged. `validate` enforces every Aggregate in exactly one Domain. `cargo run -q --bin wbs` prints the tree (`--json` available). Plan note: `.cursor/plans/blueos-2.0-domain-wbs.md` — workshop input, not a 2.0 boundary decision. Commit `6565648ee`.

- 2026-07-18: **JOURNEY ROUTE QA RUBRIC FROZEN v1.0.** Inter-rater rubric for `RouteRef` acceptance frozen in `blueos-catalog-validate/SKILL.md` → **FROZEN JOURNEY ROUTE RUBRIC v1.0**. Hard fields: resolved URL documented per sampled Known routed step; live proof (smoke `failed=0` or per-step `UnverifiedLive`); Runtime capture key = full nginx path; query params / API version / nested router prefix when `API_URL` requires them. Soft: step wording, doc line choice. Calibration set (must Pass smoke when Pi up): `probe_interface_internet_connectivity` (query param), `browse_extension_store` (kraken v2.0), `browse_video_recordings` (nested `/recorder` prefix). Calibration 2026-07-18 @ `192.168.0.177`: 4 routed steps, 4 Pass, 0 Fail. Policy: provenance line alone is NEVER sufficient for `RouteRef` acceptance.

- 2026-07-18: **JOURNEY/RUNTIME LIVE-URL GATE (required smoke).** Tier-1 `journey_http --smoke` against the live Pi (`192.168.0.177`) is now a **blocking** pre-commit gate for any journey `RouteRef`, runtime capture key, or route-resolution change — not optional QA. Motivation: smoke caught nginx/query/version bugs that provenance-only QA missed (wrong `RouteRef` paths looked correct at `file:line` but failed live). Policy: when Pi reachable, `BLUEOS_BASE=http://192.168.0.177 bash catalog/gate.sh` (or minimum `journey_http --smoke --fixtures internet,pirate,advanced`) MUST report `failed=0` before commit; when unreachable, defer commit or mark steps `UnverifiedLive` / outcomes `Unknown` — never commit `Known` `expected_status` on an unverified route. Wired into hard guardrail #9, per-service order steps 3+5, skills done-criteria, and catalog journey/runtime rules.

- 2026-07-11: **FEATURE & JOURNEY GROUPING — MULTI-APPROACH + CONSENSUS.** Applied the service-split idea one level down, to the two units a feature-first architecture actually reasons about: the 143 features and the 94 journeys. Added a generic index-based consensus engine `src/split.rs` (`modularity_partition`, `consensus_over` = pairwise agreement + union-find at majority, `group_by_key`, `clique_weights`, `components`) so features/journeys map their typed ids ↔ indices and reuse the same math (cluster.rs's service consensus left untouched to keep it green — minor, isolated duplication). **Features (`FeatureCatalog::split_consensus`)**: 4 lenses — `aggregate` (label, 19), `origin` (label, 29), `journey_cooccurrence` (modularity **Q≈0.307 POSITIVE** — unlike the negative *service* co-occurrence; capabilities used together in a workflow ARE cohesive, the hub effect lives in services not caps), `page_reachability` (Q≈0.109, coarse: page→service not page→capability, reinforcing-only). Consensus (majority≥3/4): **23 multi-feature clusters + 44 singletons**; strongest all-4/4 units = autopilot core (6), extensions (6), disk-usage (3). Sharpest re-home signal: the 7 sensor-calibration features (`calibrate_*`, `derive_sensor_calibration_status`, `detect_motor_directions`, `level_horizon`) hold together across EVERY lens yet are split today between `ardupilot_manager` and the Vehicle Setup frontend page. **Journeys (`Catalog::journey_split_consensus`)**: 4 lenses — `shared_service` (Q≈0.247), `shared_capability` (Q≈0.135, sparse=85 groups → rarely reinforces, its silence ≠ separation), `chain` (structural via `chains_from`), `dominant_aggregate` (label). Consensus: **7 multi-journey clusters + 65 singletons** (autopilot+firmware 8, extensions 7, camera streams 4, version-mgmt 4, disk 2, eeprom 2, internet 2). **Cross-validation:** the 7 journey clusters map cleanly onto the strongest feature clusters — workflow view and capability view independently draw the same boundaries. New `groupings` bin prints both; typed exports `FeatureLens`/`FeaturePairAgreement`/`FeatureSplitConsensus` + `JourneyLens`/`JourneyPairAgreement`/`JourneySplitConsensus`; 7 new tests (full-partition, determinism, agreement bounds, serde) → 58 lib tests, clippy clean, gate GREEN. Report: `.cursor/plans/blueos-2.0-feature-journey-groupings.md` (M4 input; not a decision).

- 2026-07-11: **SPLIT — MULTI-APPROACH + CONSENSUS.** Re-exercised the service split through 5 independent lenses instead of trusting one algorithm, now that F2/F3 give usage+workflow signal. Added two NEW service-level lenses to `cluster.rs` that reuse the existing modularity engine: `page_cooccurrence_matrix()` (services a page `consumes` together → +1/shared page) and `journey_cooccurrence_matrix()` (services co-participating in a journey via `services`+step routes → +1/shared journey), plus `split_lenses()` (3 coupling policies + 2 co-occurrence) and `split_consensus()` (pairwise agreement across lenses + union-find consensus clusters at majority≥3). Types `LensPartition`/`PairAgreement`/`SplitConsensus` exported; cluster bin prints all lenses + consensus; 4 new tests (full-partition, symmetry, agreement bounds, determinism) → 51 lib tests, gate GREEN. **Findings:** (1) 7 robust consensus dyads agreed by ≥3/5 lenses = defensible keep-together boundaries (ardupilot_manager↔mavlink-camera-manager, bridget↔linux2rest, cable_guy↔wifi, kraken↔versionchooser, mavlink2rest↔ping, recorder↔recorder_extractor, ttyd↔user_terminal); nothing grows past a dyad at majority. (2) page/journey co-occurrence have NEGATIVE modularity (−0.025 / −0.080) → BlueOS operator experience is hub-and-spoke around a shared MAVLink/data plane (mavlink2rest, mavlink-camera-manager, ardupilot_manager co-occur with ~everything), not separable per-domain stacks — M4 must decide hub treatment explicitly. (3) coupling_trust Q collapses to ~0 (almost all root) → privilege is not a boundary axis today. Report: `.cursor/plans/blueos-2.0-split-approaches.md` (M4 input; explicitly not a decision).

- 2026-07-11: **F3 FRONTEND JOURNEYS.** The journey spine had a blind spot symmetric to F2's: all 86 `UserJourney`s were backend/operator flows mined from docs; the multi-step wizards BlueOS 1.x runs *in the browser* (accelerometer/gyro/compass/baro calibration, level-horizon, motor-direction detection, parameter-file apply, video-stream configure) were captured only as `Page.frontend_features` + `FrontendOwned client_state`, never as first-class journeys. Decision: model them as real `UserJourney`s. Minimal type addition — `Actor::Frontend(PageId)` (additive to the `Actor` enum → zero churn to the 86 existing journeys; `route`/`RouteRef` already covers backend calls a wizard makes, pure-UI steps already use `route: None`). NO journey-level page field added (parse-don't-validate: the page is implicit in the `Actor::Frontend(PageId)` steps, and the `PageId` enum already guarantees a real page). `validate()` extended: a journey `capability_ref` is valid if it belongs to a participating service OR is registered in `capability::FRONTEND_CAPABILITIES` (frontend-origin caps are page-owned, not service-owned). Authored 3 modules (`journeys/frontend_calibration.rs` ×6, `frontend_parameters.rs` ×1, `frontend_video.rs` ×1) covering all 14 frontend-origin capabilities; 8 new `JourneyId` variants inserted alphabetically (enum + `ALL[95]` + `as_str`). Every step triangulated **Doc** (advanced/index.md: gyro 695, accel 700/702, compass 710/723, baro 739/741, level 705, motor 664, params 353, video 763/766) + **Source** (exact Vue `file:line` reused from the QA'd `vehicle_setup`/`parameter_editor`/`video_manager` page modules + verified triggers e.g. `GyroCalib.vue:47`, `FullAccelerometerCalibration.vue:170`, `MotorDetection.vue:210`, `VideoStream.vue:302`); mutating outcomes left `Grounded::unknown(...)` (never fabricated a live COMMAND_ACK / PARAM_SET / stream-create result). Independent readonly QA audited all 64 anchors: 63 PASS, 1 BOUNCE — the "ardupilot_sensors re-derives accel status" step cited `calibration.ts:33` (the COMMAND_ACK listener) → re-grounded to `store/ardupilot_sensors.ts:40` (`get accelerometers_calibrated()`), which is what the rationale already described. Soft note (left as-is, no better anchor): level-horizon Doc uses ADV:705 ("place on a level surface"), which sits under the quick-accel bullet — the docs have no dedicated board-level topic. 94 journeys, 34 frontend-actor steps, `Actor::Frontend` serializes as `{"frontend":"vehicle_setup"}`; gate GREEN (47 lib tests, clippy clean). Rule `blueos-catalog-journeys.mdc` + skill `blueos-journey-extraction` updated with the frontend-journey convention.

- 2026-07-11: **F2 FRONTEND LAYER — rubric FROZEN on 3 calibration pages.** Architect paused F1's bounded-context fold to fix the biggest blind spot: BlueOS 1.x runs whole journeys (sensor calibration, motor detection, param editing) IN the Vue frontend and holds domain state client-side; pages fan out to several services and don't map 1:1 — all invisible to the backend-only model. Decided the modeling unit is the **Page** (a router route), not the 208 components. Crate Engineer added `src/page.rs`: `Page { route/name/component/menu_title/advanced_only/stores/consumes (Observed w/ frontend file:line) + frontend_features/client_state (Asserted w/ rationale) }`, `PageServiceCall`, `ClientState`, `StateOwnership{BackendOwned,FrontendOwned,Shared}`, wired into `Catalog.pages` + validate (page→service must be cataloged or `external`; no dup PageId). New **Frontend Extractor** agent (`.cursor/skills/blueos-frontend-extraction/SKILL.md` + rule `.cursor/rules/blueos-catalog-frontend.mdc`). Calibrated on 3 exemplars spanning the range, all independently QA-ACCEPTed, **Page type unchanged across all three**: `vehicle_setup` (calibration hub — 25 consume edges [mavlink2rest PARAM/cal commands, ardupilot_manager REST, mavlink-camera-manager, 2 external], 9 client-implemented features, 13 client-state incl. FrontendOwned calibration progress + Shared param-derived "is-calibrated"); `video_manager` (12 edges to mavlink-camera-manager + commander camera_legacy, 5 features, BackendOwned stream/device lists); `disk` (backend-driven view of disk_usage, `frontend_features` intentionally EMPTY, path=Shared/usage=BackendOwned). Frozen conventions recorded in the skill: only page-triggered calls are edges (global-background store reads are noted not edged); Vuex module names for stores; single view file as component anchor; frontend features reuse shared CapabilityId (origin resolved at feature-merge via `Origin::{BackendService,FrontendPage}` — decided NOT to split the id type, no collision during scale-out); params via mavlink2rest MAVLink PARAM plane. Deferred (only if coupling needs it): typed `consume.trigger` + `endpoint.protocol`. 3 commits (types+skill+rule, vehicle_setup, video_manager+disk), gate green. **NEXT: scale to the remaining ~22 pages, then merge frontend features into the Feature inventory + re-home FrontendOwned state.**
- 2026-07-11: user_terminal FULLY MODELED — **LAST service; Phase 4 COMPLETE**. The interactive root-shell tmux session. start-blueos-core:141 tuple `'user_terminal',0,0,0,0,"cat /etc/motd"` in SERVICES→Normal, mem 0, cpu 0, run_as root; new ObservedFacts kind `Shell` (variant observed.rs:38). No nginx route, empty interfaces/listen/resources (cat /etc/motd is an ephemeral banner print, not a durable resource; ttyd's :142 `tmux attach -t user_terminal` is ttyd's concern and NOT leaked as an interface onto user_terminal). Journeys EMPTY (web-terminal journey access_web_terminal lives on ttyd; honest empty). Asserted: tier Auxiliary, 1 cap provide_interactive_root_shell, dangerous_ops=[] + user_confirmation NotRequired (KEY: the arbitrary-root-shell hazard physically lives in this session but is surfaced to USERS through ttyd, where Other("arbitrary_root_shell_execution")+Required is already modeled — QA independently confirmed ttyd carries it at ttyd.rs:295, so NOT dropped and NOT double-counted; defensible non-duplication), authorities=[] (a convenience shell is not an exclusive authority; ttyd holds web_terminal), edges=[] leaf, resources=[]. **RESOLVED ttyd's previously-deferred edge**: ttyd.rs edges changed from AssertedSet::unknown → established Edge ttyd→user_terminal via Bus::Subprocess, endpoint "user_terminal", sync Sync, required_at_boot=false (ttyd self-heals: `tmux attach -t user_terminal || tmux new -s user_terminal` at start-blueos-core:142), failure_impact Degraded (MOTD/boot session lost but shell still opens). Minimal diff on ttyd (only edges field + edge imports). QA ACCEPT first-pass. Tier-1 runtime resource-only: user_terminal tmux session alive since boot, pane idle bash PID 1040, RSS ~2.5 MB, ~0% CPU; no HTTP; no shell interaction/keystrokes (read-only). 
- 2026-07-11: **F1 FEATURE MODEL (Views A+B) COMPLETE — model inverted for 2.0.** Architect rejected all service-clustering groupings and reframed: forget the current feature↔service map, build a new one. Added `src/feature.rs` (+ `src/bin/features.rs`): the 129 grounded capabilities become service-agnostic Features (mechanical from the catalog, `validate()` enforces every capability↦exactly one Feature — none lost/invented; `origin_service` kept only as 1.x traceability). View A = 19 aggregates (grouped by the entity a feature acts on). View B = 7 journey-workflow communities from journey `capability_refs`+`chains_from` (Q≈0.238; 47 features touched by no journey). `view_divergence()` surfaces A-vs-B tensions (e.g. configure_legacy_camera[camera]↔reboot_onboard_computer[host] via the legacy-camera chain). Refactored cluster.rs to share `greedy_modularity_communities()` (service cluster bin byte-identical). Two commits, both QA-ACCEPTed (full traceability + journey-grounding verified), 43 tests, gate green. NEXT: fold aggregates/workflows into candidate 2.0 bounded contexts + author the feature→2.0-service binding (human-steered).
- 2026-07-11: **M3 CLUSTERING COMPLETE.** Replaced the all-zeros `coupling_matrix()` stub with a real engine (`src/cluster.rs`, `src/bin/cluster.rs`): weighted undirected coupling (per-bus base × failure-impact multiplier + boot bonus, over established edges only) + shared non-exclusive resources + policy affinity; committed tunable weights (`WEIGHTS_VERSION="v1"`); three policies (CouplingOnly/CouplingTrust/CouplingDomain); deterministic Clauset-Newman-Moore greedy modularity clustering; seeded-perturbation `cluster_stability()`; `boundary_proposals()`→3 candidate partitions (explicitly NOT the final answer). No new deps (hand-rolled LCG). QA independently re-ran the bin (byte-identical), verified matrix symmetry/zero-diagonal, and confirmed EVERY CouplingOnly community traces to a real edge/shared-resource (e.g. kraken↔versionchooser share /var/run/docker.sock; cable_guy↔wifi share /etc/dhcpcd.conf + dnsmasq). CouplingOnly Q≈0.245; CouplingTrust collapses to Q≈0.008 (almost every service is root — informative for the workshop). 33 tests, gate green. **This completes M0–M3; only M4 (human decisions) remains.**
- 2026-07-11: **M2 EDGE GRAPH COMPLETE (master-plan revisit).** After the 4-phase run, revisited `blueos-2.0-service-catalog.md` and found M2's "edge graph connects known static links" was still stubbed (15 services carried `edges: unknown` deferred while targets were uncataloged) and M3 `coupling_matrix()` is an all-zeros stub. Ran the edges-pass via the Card Author + independent QA harness in two groups: (A) 5 concrete Rest edges grounded in observed OutboundHttp→cataloged-service; (B) verify-then-empty the rest. Independent QA caught a real miss — ardupilot_manager's `Mavlink{role:Bridge, connect:"zenoh:0.0.0.0:7117"}`/`zenohraw:...7117` is a functional MAVLink→zenoh bridge to **zenohd**, so ardupilot_manager→zenohd (Bus::Zenoh) was added rather than left empty. Decision: log-publish `Interface::Zenoh` is ambient infra (NOT an edge; consistent with zenohd-as-hub), but a functional `Mavlink Bridge`/`OutboundHttp` to a catalog port IS an edge. `established(vec![])` (determined-empty) now used where a service has no outbound catalog coupling — distinct from `unknown` and required for the M3 matrix. Zero `edges: unknown` across all 26 services. Gate green.
- 2026-07-11: **AUTONOMOUS RUN (Phases 1-4) COMPLETE.** Phases 1-4 all DONE. Final coverage cross-check: catalog `all_observed`/`all_service_definitions`/`all_runtime` each = 26 services = 100% of start-blueos-core PRIORITY_SERVICES+SERVICES roster (autopilot→ardupilot_manager, video→mavlink_camera_manager, versionchooser present). 35 runtime-capture artifacts (some services have Tier1+Tier2 or multi-condition captures). Every service four-layered, `bash gate.sh` GREEN (fmt/clippy/tests+validate/extract/drift/export), committed one-per-service with post-commit ledger update. New harness precedents established this run: Bus::File shared-filesystem producer→consumer edge (recorder→recorder_extractor) and Bus::Subprocess tmux-attach edge (ttyd→user_terminal), plus ObservedFacts kind Shell. No un-DONE phase-step/ledger row remains.

- 2026-07-11: recorder FULLY MODELED (9th binary; external Rust binary blueos-recorder). start-blueos-core:145 SERVICES→Normal, mem 250, cpu 0, run_as root, no nice; entrypoint `blueos-recorder --recorder-path /usr/blueos/userdata/recorder`. Installed via core/tools/recorder/bootstrap.sh (PROJECT blueos-recorder, VERSION 0.0.5, repo github.com/bluerobotics/blueos-recorder → /usr/bin) — EXTERNAL binary, no in-repo source. Observed: NO nginx route (nginx /recorder-extractor/ :129 is the DIFFERENT already-cataloged service — did NOT confuse), interfaces=[] + listen=[] (launch has NO --zenoh/--connect/port flag → asserting a Zenoh interface would be fabrication; left empty honestly), resource /usr/blueos/userdata/recorder SharedWrite (from --recorder-path). git_path/logs/zenoh/sentry/openapi/aliases/nice all Unknown (external binary). Journeys EMPTY vec![] (background daemon, no operator UI/nginx; all operator interaction via recorder_extractor's Records gallery — searched docs (no blueos-recorder/mcap grounding) + frontend (only recorder_extractor's /tools/records) → honest empty, like iperf3). Asserted: tier Auxiliary (vehicle flies without recording), 1 machine cap record_vehicle_data_stream, dangerous_ops=[] (MCAP writes additive, not destructive — contrast recorder_extractor delete_recording), authorities Other("session_recorder") (exclusive MCAP producer; UserdataWriter rejected since extractor also writes MP4s to the dir), privilege Root. **EDGE: recorder→recorder_extractor via Bus::File (real variant edge.rs:27), endpoint /usr/blueos/userdata/recorder, Async, failure_impact Degraded — FIRST filesystem/producer→consumer edge in the catalog. required_at_boot corrected true→false on QA bounce (recorder_extractor serves existing MCAP/MP4 files without the recorder daemon running; RECORDER_DIR at recorder_extractor/main.py:27).** QA one bounce (edge required_at_boot overclaim) then verified ACCEPT on all else. Tier-1 runtime resource-only (no HTTP interface): PID 1272, RSS ~9.4 MB, **CPU ~6% mean even when idle** (non-zero — daemon actively polling its source), recording dir /usr/blueos/userdata/recorder EMPTY at capture (idle, no active recording). Deployed binary reports v0.1.0 (bootstrap.sh pins 0.0.5 — Pi master runs newer; observed layer keeps repo's 0.0.5 source truth, runtime note records the v0.1.0 discrepancy). Nothing written to the recorder dir (read-only). NEW PATTERN: Bus::File cross-service data-flow edge is now the precedent for shared-filesystem producer→consumer couplings.

- 2026-07-11: filebrowser FULLY MODELED (8th binary, Go). Web file manager (upstream github.com/filebrowser/filebrowser). start-blueos-core:137 SERVICES→Normal, mem 250, cpu 0, nice -19, run_as root. Observed: Rest /file-browser/ :7777 (nginx.conf:120 location + :121 proxy_pass, baseurl /file-browser corroborated at :137), resource /etc/filebrowser/filebrowser.db SharedWrite (from --database). NO served-root fabricated (no --root arg). logs/zenoh/sentry/git/openapi/aliases Unknown. 1 journey manage_blueos_files (File Browser page, Visibility::Advanced per menus.ts:42 advanced:true; embedded upstream SPA so ALL steps route:None — internal /file-browser/api/ calls are not BlueOS routes; grounded in docs advanced:431 "viewing, editing, downloading, uploading BlueOS files" + FileBrowserView.vue:19 service_path). Asserted: tier Auxiliary (file-mgmt convenience, SSH/scp/ttyd fallback, danger≠tier), **dangerous_ops=Other("arbitrary_file_mutation") + user_confirmation Required (edit/delete/move arbitrary root-owned files, irreversible — nginx webdav_file_mutation precedent)**, privilege Root, authorities Other("file_browser") (exclusive full file-manager UI vs nginx's narrow /upload/ DAV; no first-class Authority fits), edges=[] (self-contained SPA, only inbound Rest :7777), 1 cap manage_blueos_files. QA ACCEPT first-pass. Tier-1 runtime: PID 921, idle 0% CPU / ~15.9 MB RSS; GET /file-browser/ 200 (SPA HTML, p50 2.8ms), /file-browser/health 200 ("status":"OK", p50 1.7ms — real health endpoint), /api/renew 401 (auth-gated, recorded not baselined). No auth attempted, no mutating APIs touched. Journey steps kept route:None (SPA, no exact probe).

- 2026-07-11: iperf3 FULLY MODELED (7th binary). Raw network-bandwidth server. start-blueos-core:135 SERVICES→Normal, mem 250, cpu 0, run_as root, no nice. Observed: listen [5201] (--port 5201), nginx_prefixes=[] (NO nginx route — verified no 5201/iperf in nginx.conf), interfaces=[] (raw iperf TCP protocol — Interface enum {Rest,Zenoh,Mavlink,Websocket,HttpStream,OutboundHttp,Subprocess,File,Settings,Hardware,Docker} has NO fitting variant; NOT shoehorned into Rest/Websocket — port captured in listen + entrypoint). All else Unknown. FIRST service with ZERO journeys — journeys()=vec![] (verified: rg iperf/5201 → 0 hits in BlueOS-docs + frontend; bandwidth testing is documented via pardal's HTTP speed test, NOT iperf). Asserted: tier Auxiliary (diagnostic like pardal/disk_usage), journey_refs=[], 1 machine cap serve_iperf_bandwidth_test, dangerous_ops=[] (passive measurement; saturation is a failure_mode not an irreversible op), user_confirmation NotRequired, authorities=[] (non-exclusive measurement server, consistent with pardal), privilege Root, edges=[] (standalone inbound-only), resources Unknown. QA ACCEPT first-pass (noted non-blocking: empty nginx_prefixes has no negative-evidence citation — acceptable). Tier-1 runtime: PID 772, idle 0% CPU / 1.3 MB RSS (60 samples), TCP LISTEN *:5201 confirmed via ss (health = listening socket, not HTTP). No throughput test run (would saturate vehicle LAN). No HTTP SLO baselines (not HTTP). CONFIRMED empty-journey services validate cleanly (validate() does not require a journey per service).

- 2026-07-11: ttyd FULLY MODELED (6th binary). Web terminal (browser root shell). start-blueos-core:142 SERVICES→Normal, mem 250, cpu 0, nice -19, run_as root. Observed: Interface::Websocket /terminal/ :8088 (nginx.conf:213/214 proxy + :216 Upgrade headers → Websocket not Rest), Interface::Subprocess sh -c "tmux attach -t user_terminal || tmux new -s user_terminal" (:142). logs/resources/zenoh/sentry/git Unknown (attaches existing tmux, no config/data). 1 journey access_web_terminal (Terminal page, Visibility::Advanced per menus.ts:117 advanced:true — corroborated by pirate block in docs advanced:617-619; websocket step route:None like zenohd; interactive-shell outcome Grounded::unknown). Asserted: tier Auxiliary (dev convenience, SSH fallback, SERVICES comment says not fundamental — DANGER≠TIER, danger goes in dangerous_ops), **dangerous_ops=Other("arbitrary_root_shell_execution") + user_confirmation Required — the HIGHEST-IMPACT capability in the catalog (full interactive root shell over unauthenticated LAN websocket, far beyond recorder_extractor's single file delete)**, privilege Root (runs as root AND exposes root shell), authorities Other("web_terminal") (no first-class Authority fits), edges=Unknown (attaches user_terminal tmux @ start-blueos-core:141 — user_terminal not yet cataloged, deferred like bridget→linux2rest; NOT [] since a real dep exists, NOT a fabricated edge). QA ACCEPT first-pass. Tier-1 runtime: PID 1142, idle ~1.3 MB RSS ~0% CPU (no session attached); GET /terminal/ 200 (HTML, p50 10.6ms), /terminal/token 200 (token JSON, value not recorded). SAFETY: interactive websocket shell NOT opened (would exec as root); journey shell outcome kept Unknown.

- 2026-07-11: nginx FULLY MODELED (5th binary, C not rust). HTTP reverse-proxy front door. start-blueos-core:143 SERVICES→Normal, mem 250, cpu 0, nice -18, run_as root (master; www-data workers per nginx.conf:1). Observed models ONLY nginx-owned locations (NOT proxied service prefixes): Rest / :80 (nginx.conf:51, frontend SPA root /home/pi/frontend), Rest :2770 (legacy Companion redirect), OutboundHttp https://$target (/cache/ external caching proxy, resolver 8.8.8.8, nginx.conf:73/67), File write /usr/blueos/ (WebDAV /upload/ dav_methods PUT DELETE MKCOL COPY MOVE, nginx.conf:290/297), File read /usr/blueos (/userdata/, nginx.conf:301). logs_path /var/log/nginx (real). Resources: config, frontend static, /usr/blueos RW, /var/cache/nginx, /var/log/nginx. 1 journey access_blueos_web_interface (getting-started docs: blueos.local browser access; Visibility::Default doc-grounded since nginx has no menus.ts entry — same doc-only basis as beacon). Asserted: tier Important (all web access depends, flight control via MAVLink/GCS does not route thru nginx), **dangerous_ops=Other("webdav_file_mutation") + user_confirmation Required** (/upload/ DAV DELETE/MOVE irreversibly mutates /usr/blueos over unauthenticated LAN — recorder_extractor precedent), privilege Root, offline_required true (/cache/ optional), authorities NginxProxy (FIRST-CLASS enum @ service.rs:45), edges=[] (ingress hub; per-service proxy routes are structural ingress of each proxied service, /cache/ target is external internet — same hub treatment as zenohd), 5 caps (access_blueos_web_interface + serve_frontend_spa + reverse_proxy_backend_services + serve_webdav_uploads + cache_external_http). QA BOUNCED health string (claimed /status reflects backend reachability, but nginx.conf:62-63 is unconditional `return 204` = nginx liveness only) → narrowed to "GET /status returns 204 when nginx is online" citing :60-63. Tier-1 runtime: MULTI-PROCESS master PID 1178 (root, idle ~0% CPU, 5.5 MB) + 5 www-data workers (PIDs 1216-1220, ~16.5 MB agg, total ~22 MB); GET /status 204 (0.5ms), / 200 SPA (0.8ms), /userdata/ 200 (0.8ms), /assets/ 200 (3.9ms autoindex); /upload (WebDAV write) + /cache (external) NOT probed. Re-grounded journey GET / (200) + GET /status (204) outcomes; left blueos.local browser step None. NOTE: service.rs already has first-class Authority variants ZenohBroker + NginxProxy (pre-existing) — used both for zenohd/nginx.

- 2026-07-11: zenohd FULLY MODELED (4th binary). Zenoh pub/sub message-bus router. start-blueos-core:128 SERVICES block → Normal tier, mem/cpu 0, run_as root. Observed: Rest /zenoh/ :7117 (nginx:246 + json5 rest.http_port), Websocket /zenoh-api/ :7118 (nginx:261 + json5 remote_api.websocket_port), resources = backend fs root $TOOLS_PATH/zenoh (SharedWrite) + config blueos-zenoh.json5 (SharedRead). zenoh_log_topic/sentry/git_path/logs_path/openapi Unknown. NO fabricated native 7447 (absent from config/nginx). 1 journey inspect_zenoh_network (Zenoh Inspector page, Visibility::Advanced per menus.ts:145 advanced:true; websocket connection to /zenoh-api/ modeled with route:None since zenoh protocol ≠ HTTP RouteRef; live-data outcomes Unknown). Asserted: tier Important (broadly-depended bus but flight control routes via MAVLink not zenoh), dangerous_ops=[] (inspector read-only, adminspace not a destructive human journey), privilege Root, authorities ZenohBroker (FIRST-CLASS enum variant at service.rs:46 — not Other()), edges=[] (hub; all connections inbound from publisher/subscriber services, none outbound), +1 machine cap route_pubsub_messages (omitted aggregate_service_logs — zenohd routes, doesn't aggregate). QA ACCEPT first-pass. Tier-1 runtime: PID 426, RSS ~42 MB, BURSTY routing CPU (median 4%, p95 59%, max 76% — routes all inter-service pub/sub + logs); adminspace GET /zenoh/@/local/router 200 (p50 2.5ms, "sessions"), @/router/local 200 ([] no peers), @/** 200 (p50 107ms, heavier full-keyspace query); empty /zenoh/ = 400 (needs key expr). Left Inspector live-topology journey outcomes Unknown (adminspace probes don't exercise the topic-message websocket flow).

- 2026-07-11: HARNESS FIX — extract self-check (find_extracted) now falls back to the observed tmux_name so binaries whose catalog id differs from the start-blueos-core tmux name still get tier/mem/cpu cross-checked. Motivated by `video` (tmux) vs `mavlink-camera-manager` (id). Committed standalone before modeling video.
- 2026-07-11: video/mavlink-camera-manager FULLY MODELED (3rd binary). tmux `video`, catalog id `mavlink-camera-manager`, aliases=["video"]. start-blueos-core:120 PRIORITY block, mem/cpu 0, nice --19, run_as root. Observed: Rest :6020 (/mavlink-camera-manager/, unversioned), Mavlink Consumer tcpout:127.0.0.1:5777, OutboundHttp stun://stun.l.google.com:19302, logs_path /var/logs/blueos/services/mavlink-camera-manager (REAL --log-path, citable — unlike other binaries). zenoh_topic/sentry/git_path/resources/openapi Unknown. /webrtc/ws/->:6021 NOT in observed (nginx doesn't name MCM there) — asserted-layer only. 4 journeys (view_camera_streams, configure_camera_stream, remove_camera_stream, configure_uvc_device_controls) grounded in docs + video-manager frontend; view_webrtc_stream OMITTED (BlueOS docs point to Cockpit, no in-app WebRTC player). Asserted: tier Important (operator-critical video, autopilot flies without it), dangerous_ops=[] (stream removal is reversible config, unlike recorder_extractor's irreversible file delete), privilege Root, offline_required true (STUN optional), authorities camera_stream_manager, +2 machine caps (provide_webrtc_signalling, advertise_cameras_over_mavlink). **2nd concrete edge: mavlink-camera-manager → ardupilot_manager via paired tcpout:5777 / tcpin:5777 (ardupilot_manager observed endpoint @ autopilot_manager.py:111).** QA BOUNCED 3 journey provenance anchors (POST /v4l cited store:190 [thumbnail sizing] not VideoControlsDialog.vue:189; device-list step VideoManager.vue:8 [css class] not :12 [v-for]; bundled nickname/encoding/resolution/framerate step at dialog:27 [encoding only] → split into 4 steps @ :20/:27/:35/:42). Orchestrator verified each against source and fixed directly (added file-parametrized sourced_route_in helper). Tier-1 runtime: PID 795, RSS ~37 MB, CPU ~0.6% (no active pipelines); NO USB/external cameras attached → GET /v4l 200 lists 4 onboard nodes (bcm2835-isp x2, Fake source, Redirect source) p50 8.5ms, GET /streams 200 empty [] p50 1.7ms; /info=201 (version 0.2.4), /xml=400 (needs file param); mutating routes left Unknown (no camera hw). Re-grounded the 2 read-only journey outcomes to runtime, left 3 mutating ones Unknown.

- 2026-07-11: linux2rest FULLY MODELED (2nd binary). Rust binary (start-blueos-core:136, SERVICES block → Normal tier, mem 250, run_as root), nginx /system-information/ :6030, UNVERSIONED (routes directly under prefix, versions=[]). Read-only host system-info REST (cpu/memory/disk/network/temperature/process/platform/serial/netstat/usb). Observed binary adaptation: kind=Binary, zenoh/sentry/logs_path=Unknown, sole Rest interface (no OutboundHttp/Mavlink/File — /proc reads are upstream internals). 1 journey (view_system_information, System Information page, doc+frontend-grounded). Asserted tier Important (bridget serial-enum + helper depend on it; NOT flight-critical), dangerous_operations=[] (read-only), authorities Other("system_information_provider") (exclusive host-info consolidator), edges=[] (reads local host only; bridget/helper edges are THEIR outbound, not linux2rest's), 2nd capability provide_system_information_over_rest (machine consumers). QA ACCEPT first-pass. Interrupted/resumed cleanly (observed+journeys were in-flight uncommitted; removed a stray catalog/examples/print_validate_errors.rs debug file a subagent left). Tier-1: PID 855 (rust, RSS flat 28 MB, CPU ~2.5% spiky on sampler ticks), 9 read-only GETs all 200; /system p50 10ms + /system/process p50 9ms heaviest, rest ~1.5ms. No mutating routes exist. Capture image = sha256:0406983a (master moved on).

- 2026-07-11: mavlink2rest FULLY MODELED — FIRST BINARY, FIRST CONCRETE EDGE. Rust binary (start-blueos-core:121, PRIORITY_SERVICES block → tier Priority), nginx /mavlink2rest/ :6040. MAVLink↔REST/WS bridge: connects out to router udpout:127.0.0.1:14001 (MavlinkRole Consumer), serves REST on [::]:6040. Observed binary adaptation: kind=Binary, zenoh/sentry/logs_path=Unknown (not python), versions=[] (no fabrication). 1 journey (MAVLink Inspector, UI-only steps, doc-grounded ADV:496-506). Asserted tier Important (data-plane bridge; many dependents break but vehicle still controllable via router/GCS → NOT VehicleCritical), dangerous_operations=[] (transparent relay — POST-send risk captured in blast_radius + mavlink_send_surface_abuse failure_mode, not a service-owned dangerous op), authorities Other("mavlink_rest_bridge"). **EDGE mavlink2rest→ardupilot_manager** (Mavlink, udpout/udpin:14001, required_at_boot true, failure_impact ServiceUnavailable) — verified BOTH sides in source: mavlink2rest Consumer udpout:14001 ↔ ardupilot_manager UDPServer udpin:14001 (autopilot_manager.py:74-81, protected). QA ACCEPT first-pass (no bounce). Tier-1: PID 243 (rust binary, RSS flat 8.8 MB — vs ~40 MB python; CPU ~1.3%), GET /v1/mavlink p50 3.9ms → 200 message tree, GET / p50 1.3ms → 200 html (built-in Vue UI), GET /v1/mavlink/HEARTBEAT → 200 body "None" (no live heartbeat, no vehicle armed). POST /v1/mavlink + WS NOT exercised (injects MAVLink). Runtime Specialist extended sample_resource.sh with --pid. FOLLOW-UP: ardupilot_manager/nmea_injector/ping edges to mavlink2rest can now be resolved from Unknown (deferred edges-pass).

- 2026-07-11: recorder_extractor FULLY MODELED. Root FastAPI v1.0 (nginx /recorder-extractor/ :9150, router prefix /recorder). Serves recorded MP4 for playback/download + manages /usr/blueos/userdata/recorder; background loop (every 10s) extracts MP4 from MCAP via 5 subprocs: mcap doctor, mcap recover, mcap-foxglove-video-extract, gst-discoverer-1.0, gst-play-1.0. 3 journeys: browse/download/delete. **FIRST recent card with NON-empty dangerous_operations**: DELETE /files → path.unlink() = Other("delete_recording") (irreversible data loss; precedent = disk_usage's Other("delete_filesystem_paths")); user_confirmation Required (rubric policy; frontend currently has NO confirm dialog → framed as 2.0 policy assertion, honestly noted). Asserted Auxiliary, Root, authorities Other("video_recording_server"), edges Unknown (shares recorder dir w/ uncataloged `recorder` svc — SharedWrite resource, deferral convention). QA BOUNCE ×1: delete journey summary composite string ("MP4...Records gallery...destructive and irreversible") grounded at main.py:397 which only says "Delete a recording." → shortened summary to match line literally. Tier-1: PID 1323 (root), RSS flat 35.6 MB, CPU ~0.3% (10s MCAP loop idle, no files); GET /files p50 6ms → 200 [], GET /status p50 6ms → 200 {"processing":[]}, GET / p50 5ms. Thumbnail/download/DELETE NOT exercised (no recordings present / destructive).
- 2026-07-11: MILESTONE — all 18 PYTHON/aiohttp services FULLY MODELED. Remaining Phase-4 work = 9 external BINARIES (mavlink2rest, linux2rest, video/mavlink-camera-manager, zenohd, nginx, ttyd, iperf3, filebrowser, recorder) + user_terminal (trivial: `cat /etc/motd`). Binaries need a different extraction approach (no python main.py): anchor observed facts in start-blueos-core (entrypoint/args/tier/port/run_as), nginx.conf (prefix), and upstream repo/docs for the binary's behavior. Several are Rust binaries maintained by BR (mavlink2rest, mavlink-camera-manager, and zenohd is eclipse). Journeys for infra binaries may be thin/None (they are plumbing consumed by other services) — model honestly, use Unknown/None where no operator-facing journey exists.

- 2026-07-11: bridget FULLY MODELED. User-configured serial↔UDP bridge manager ("Serial Bridges", nginx /bridget/, VersionedFastAPI v1.0 on :27353). Non-root (run_as blueos → PrivilegeLevel::Regular, like ping). Spawns the same `bridges` BINARY per operator-defined bridge; enumerates serial ports by proxying linux2rest (OutboundHttp localhost:6030/serial). Persists bridge list to settings-2.json. 3 journeys (Advanced/pirate visibility): view/create/remove. Asserted Auxiliary, dangerous_operations=[] (reversible operator config, consistent w/ ping/cable_guy; serial-exposure + port-contention risk → failure_modes/blast_radius), authorities Other("serial_bridge_manager") (distinct from ping's ping_sonar_manager — arbitrary operator serial devices vs BR sonar autodetect, NO overlap), edges Unknown (linux2rest not cataloged — deferred, same convention as m2r). GAP: dynamic per-bridge UDP ports + serial devices have no Interface variant. QA BOUNCE ×3 (journey-only UI anchor precision): (1) card step claimed serial-path+baud+UDP from BridgeCard.vue:22 (only name+baud) → reworded to name+baud; (2) "select serial+baud" from CreateDialog:18 (serial select only; baud at :81) → split into serial(:17)+baud(:81) steps; (3) "choose UDP mode/IP/ports" cited ADV:586 (prose about 0.0.0.0 server, not a UI action) → re-anchored to CreateDialog tabs :87 + IP field :111. Tier-1: PID 633 (python3, blueos), RSS flat 38 MB, CPU ~1.7% (spiky to 79%); GET /v1.0/bridges p50 9ms → 200 [], GET /v1.0/serial_ports p50 24ms → 200 [ttyAMA0..3,ttyS0] (latency incl. linux2rest round-trip), GET / p50 6ms. POST/DELETE NOT exercised (mutating, no serial HW).

- 2026-07-11: ping FULLY MODELED. Blue Robotics Ping-family sonar manager (nginx /ping/, VersionedFastAPI v1.0 on :9110). **FIRST non-root Python service** — wrapped in RUN_AS_REGULAR_USER_BEGIN/END → run_as=blueos → asserted PrivilegeLevel::Regular (all prior py svcs were Root). Auto-detects Ping1D/Ping360 over serial + Ping360-over-ethernet (UDP discovery :30303); spawns the external `bridges` BINARY (which("bridges"), NOT the bridget service) per device → UDP endpoint (9090↓ Ping1D / 9092↑ Ping360) for surface Ping Viewer. Optional Ping1D→MAVLink DISTANCE_SENSOR via OutboundHttp localhost:6040 (m2r). 3 journeys. Asserted Auxiliary, dangerous_operations=[] (MAVLink distance toggle reversible, consistent w/ nmea_injector GPS_INPUT; depth-hold risk → failure_modes/blast_radius), authorities Other("ping_sonar_manager"), edges Unknown (m2r not cataloged, nmea convention). GAP: dynamic per-device UDP bridge ports have no Interface variant (same class as nmea_injector). QA BOUNCE ×2 (journey-only): (1) connect journey visibility cited OVERVIEW:107 (Feature Comparison heading) → re-anchored to menus.ts:94 `advanced:false`; (2) device-card step claimed type+UDP+serial from ADV:563 (serial only) → re-anchored/reworded to ping1d.vue:15 (Bridge UDP endpoint). Tier-1: PID 1072 (python3, blueos user), RSS flat 40 MB, CPU ~1% (1s serial poll + eth discovery, spikes to 26% during discovery bursts); GET /v1.0/sensors p50 11ms → 200 `[]` (no sonar HW attached), GET / p50 6ms. POST /sensors + UDP bridge + Ping Viewer + MAVLink fwd NOT exercised (mutating / no hardware).

- 2026-07-11: pardal FULLY MODELED. Network speed-test service (nginx /network-test/, NOT /pardal/; unversioned aiohttp on :9120). Two mechanisms: LAN throughput via self-hosted HTTP /get_file (download) + /post_file (upload) + /ws echo (latency); WAN via speedtest-cli library (/internet_* routes) — NO iperf3 subprocess (iperf3 is a SIBLING tmux service) and NO literal OutboundHttp URL (speedtest-cli hides them). Asserted Auxiliary, dangerous_operations=[] (bandwidth saturation is transient/reversible, captured in blast_radius), authorities=[] (measurement producer, no exclusive right — consistent with disk_usage), offline_required true (LAN test offline-capable). QA BOUNCE ×2, both fixed: (1) /ws step used Actor::Service with a frontend client anchor → re-anchored to service handler main.py:45; (2) Interface::Rest anchored at web.Application() (line 144, shows neither prefix/port/version) → re-anchored to the TCPSite bind main.py:155 (carries port; prefix corroborated by nginx.conf:197). NOTE: unversioned service → versions:[]; the "cite prefix_format line" convention doesn't apply, so anchor the bind line. Tier-1 capture: PID 1008 `python` (NOT python3 — extended sample_resource.sh to match both), RSS flat 38.8 MB, CPU ~0.02% idle; GET / p50 2.6ms, internet_test_previous_result p50 2.7ms (returned 200 not 500 — SPEED_TEST initialized at boot on this host), get_file?size=1MB p50 82ms (transfer-dominated). WAN speedtest routes + POST upload + /ws NOT exercised (link-saturation hazard / mutating).

- 2026-07-11: nmea_injector FULLY MODELED. External NMEA/GPS ingest → MAVLink GPS_INPUT via HTTP POST to mavlink2rest (NOT a MAVLink connect string; OutboundHttp localhost:6040). Asserted Auxiliary (opt-in external GPS, not required for flight), nmea_gps_injector authority (producer, NOT router owner), dangerous_operations=[] (socket config reversible; wrong-position is a failure_mode). edges Unknown (mavlink2rest not yet cataloged — deferred). Extractor fixed nothing (clean); 5 dns-style anchors N/A. Harness gap: dynamic per-socket NMEA TCP/UDP listeners have no Interface variant. Tier-1 capture: RSS ~40 MB, CPU ~0.26%; GET /socks p50 8ms (returned [], no sockets configured). Mutations NOT exercised.

- 2026-07-11: customization FULLY MODELED. White-labeling (theme color, logo/vehicle-image branding, .glb 3D model overrides) writing /usr/blueos/userdata/{styles,branding,modeloverrides}. Asserted Auxiliary (cosmetic, non-critical), ui_branding_manager authority (scoped to userdata assets; bag stores sidebar images separately — noted overlap), dangerous_operations=[] (.glb upload is static data served to renderer, NOT executed code ⇒ NotRequired). 8 journeys. Self-QA (simple service): verified theme route + doc + frontend paths + authority uniqueness. Tier-1 read-only capture: RSS ~35 MB, CPU ~0.26%; all GETs ~5-6ms. Mutations NOT exercised.

- 2026-07-11: bag_of_holding FULLY MODELED. Generic JSON key-value store (set/get/overwrite, appdirs db.json). Only 1 first-class operator journey (modify_bag_database via advanced Bag Editor); all other frontend use (settings/wizard/vehicle-image/cloud-token) is INDIRECT infra — journeys belong to consuming features, not the store (Docs Specialist correctly declined to invent journeys). Asserted Important (many features depend on it; flight-independent), json_document_store authority, dangerous_operations=overwrite_entire_datastore (whole-db replacement, ⇒ Required; incremental /set excluded). Self-QA (simple service): verified journey anchors + /overwrite route. Tier-1 read-only capture: RSS ~35.8 MB (lightest Python service), CPU ~0.49%; GET /get/* p50 7ms. set/overwrite NOT exercised.

- 2026-07-11: versionchooser FULLY MODELED. Core BlueOS Docker-image updater (pull/switch/delete/restart, bootstrap startup.json, docker login) via docker.sock. Asserted Important (system-integrity critical, not flight-critical), is_platform=false (manages core OS image, not a third-party extension host — contrast kraken=true). dangerous_operations = Upgrade + delete_core_image + pull_untrusted_image ⇒ user_confirmation Required (QA accepted; pull mirrors kraken's install_arbitrary_docker_image). Authorities blueos_version_controller / bootstrap_image_controller / UserdataWriter(startup.json) — distinct from kraken's extension orchestrator (core image vs extension image split). QA bounced 1 anchor (zenoh_log_topic main.py:12 → logs.py:78); fixed. Tier-1 read-only capture: RSS ~53 MB, CPU ~0.82%; version/bootstrap GETs ~90-105ms (docker.sock). Mutating/destructive routes NOT exercised.

- 2026-07-11: wifi FULLY MODELED. WLAN manager (wpa_supplicant Bullseye / NetworkManager Bookworm handlers). Asserted Important, 2 authorities (wireless_network_controller, wifi_hotspot_operator) — distinct from cable_guy's wired authorities. dangerous_operations=[] (reversible reconfig, same rule as cable_guy). QA PASS on first submit. Real cross-service finding: wifi + cable_guy BOTH run dnsmasq and edit /etc/dhcpcd.conf (wifi=uap0 hotspot, cable_guy=wired); authority strings distinct so validator OK; narrowed cable_guy's dnsmasq authority rationale to "WIRED interfaces" for accuracy. Harness gaps: wpa_supplicant control-socket protocol, NetworkManager D-Bus, TCP fallback have no Interface variant. Tier-1 read-only capture: RSS ~46 MB, CPU ~0.37%; GET /status ~30ms (wpa ctrl socket). Mutating routes NOT exercised.

- 2026-07-11: cable_guy FULLY MODELED. Priority-tier wired-network manager; asserted Important, 3 authorities (wired_network_controller, host_dns_writer, onboard_dhcp_server_operator), dangerous_operations=[] (network reconfig is REVERSIBLE per frozen rubric rule 2; lockout is a failure_mode, not a dangerous op — QA confirmed). Modeling gaps logged: pyroute2 netlink + NetworkManager D-Bus in-process mutation have no Interface variant (only subprocess/file captured). Fixed 5 mis-anchored dns.py subprocess lines (extractor cited wrapper-call lines, not the run_command lines: correct = 56/64/74/80/86). QA over-strictly bounced the Interface::Rest anchor (main.py:163 prefix_format) — but that matches the QA-passed helper.rs gold standard; kept it and documented the convention in the extraction skill. Tier-1 read-only capture: RSS ~53 MB, CPU ~2.47%; GET /host_dns is slow (~1.2s, shells out to cat/lsattr resolv.conf). Mutating routes NOT exercised (lockout hazard).

- 2026-07-11: beacon FULLY MODELED. mDNS/zeroconf advertisement has no `Interface` variant — modeled honestly as capability `advertise_mdns_domains` + `discover_blueos_on_network` journey; logged as harness gap (consider `Interface::Mdns` if more network-advertisement services appear). QA bounced 2 anchors (Settings evidence → pykson_manager.py:69; discover precondition → getting-started:29); both fixed. Tier-1 read-only capture: RSS ~39.9 MB flat, CPU ~0.79% mean; GET SLOs p50 5–10.5 ms. POST /vehicle_name + /hostname NOT exercised (would rename live vehicle); those journey outcomes stay Unknown.

- 2026-07-11: Kraken committed `e5ffdaf8a`. Runtime captured Tier 1+2 with Example 1; vehicle restored. Fixed `probe_http.sh` (ARG_MAX + SIGPIPE). 4 journey outcomes left Unknown → **Phase 1 target**.
- 2026-07-11: `SloBaseline` is latency-only; `ResourceUsage` holds cpu/mem Distributions; kraken has NO state machine (state_contracts Unknown is correct).
- 2026-07-11: Journey validation requires participating service + capabilities to exist → wire `all_journeys()` together with the Card Author step, not before.
- 2026-07-11: Phase 3 done. Added `catalog/gate.sh`; hardened QA skill (extract + frozen rubric). Audit found no stale refs; validator is strong. Deferred coverage-threshold calibration and route-inventory cross-check with rationale (see PHASE 3 AUDIT). Harness is ready for Phase 4 batch.
- 2026-07-11: Phase 2 (4) done. Inter-rater calibration: independent rater vs committed gold on ardupilot+kraken. Hard enum/bool agreement 13/14 (only kraken user_confirmation flipped). All disagreements (that flip + dangerous_ops/authorities label variance) were rubric under-specification, not data errors. Froze rubric v1.0 with 4 deterministic decision rules; committed cards already comply, so no data changes. Also fixed stale tier menu + user_journeys→journey_refs in the card skill.
- 2026-07-11: Phase 2 (2)+(3) done. extract self-check + real drift landed. Adjudicated a REAL drift finding: drift flagged ardupilot `api_stable:true` vs runtime 500 in `stopped`. Ruled the CHECK unsound (api_stable = versioned surface stability; contractual 5xx are documented StateContracts, not surface churn). Removed that check; strengthened the state-machine/state existence check instead. Reworded the artifact note that conflated the two meanings of "stable". Lesson: runtime↔asserted checks must compare like-for-like semantics.
- 2026-07-11: Phase 1 done. Closed kraken's 4 gaps with a reversible Tier-2 capture of Example 1: custom install (POST /extension/ 200), add/del manifest (POST 201 / DELETE 204), tag edit v1.0.0→v1.0.1 (PUT 200), container log (GET 200). Artifact `transitions` extended; 4 journey outcomes re-grounded. Values orchestrator-captured + verified against artifact; gates green.

## PHASE 3 AUDIT — bulletproofing findings

Bulletproofed (done):
- **One-shot gate** `catalog/gate.sh` — deterministic full gate; run it after every slice and on every resume. `extract` now hard-fails if a newly-modeled observed layer diverges from `core/start-blueos-core` (catches copy/paste errors automatically).
- **QA skill** runs `extract` + applies the frozen rubric to judgment fields.
- **Completeness enforcement** is via mandatory `Unknown{reason}` + QA provenance spot-check (one fabricated citation = BOUNCE), not a numeric gate.

Deliberate deferrals (documented; low ROI until more services exist):
- **Coverage threshold** stays advisory (10_000). Phase-4 services legitimately carry Unknowns (`team`, `adr_refs`, `compatibility_policy`). Revisit AFTER all services are modeled to set a data-driven threshold; completeness is meanwhile enforced by mandatory Unknown reasons + QA.
- **Route-inventory cross-check**: `RouteRef`s in journeys/runtime are not validated against each service's real OpenAPI routes (validate.rs notes this). Mitigation today: runtime-capture skill grounds routes from live OpenAPI + QA spot-check. Future: store per-service route inventory in `observed.openapi_refs` and cross-check.
- **`Interface::Settings`** carries only `path` (root shape lives in prose/rationale).
- **No mDNS/zeroconf `Interface` variant**: beacon advertises BlueOS over mDNS (zeroconf lib) but the `Interface` enum has no network-advertisement variant. Currently captured via the service purpose + a capability, not an interface. Future: consider an `Interface::Mdns`/`NetworkAdvertisement` variant (schema change).

## OPEN HARNESS GAPS (superseded by PHASE 3 AUDIT above; kept for history)

- DONE: `extract` binary implemented (start-blueos-core parser + observed self-check). Future: could also mechanize nginx-prefix / listen-port extraction (deferred: nginx→service mapping is ambiguous by port/upstream).
- DONE: `drift` now covers asserted↔observed (resource evidence) and asserted↔runtime (state-machine/state existence). Note: `api_stable` has no sound runtime status falsifier (surface stability ≠ runtime robustness) — intentionally not checked.
- Coverage gate threshold is `10_000` (effectively disabled) — calibrate after rubric freeze.
- Inter-rater reliability not yet measured; rubric not frozen (Phase 2 item 4).
- `Interface::Settings` carries only `path` (no root-shape field) — settings root shape currently lives in prose/rationale.
