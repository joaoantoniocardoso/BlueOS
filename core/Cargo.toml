[workspace]
resolver = "2"
members = [
    "app/blueos",
    "libs/api",
    "libs/idl",
    "libs/adapters/cli",
    "libs/adapters/comms",
    "libs/adapters/comms/channel",
    "libs/adapters/comms/driver",
    "libs/adapters/comms/zenoh",
    "libs/adapters/logging",
    "libs/adapters/settings",
    "libs/app/service",
    "libs/logic/cqrs",
    "libs/logic/jobs",
    "services/example/adapters/simulated_pump",
    "services/example/app",
    "services/example/logic/pump",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[profile.dev]
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false

[profile.release]
lto = "thin"
panic = "unwind"
strip = true

[workspace.dependencies]
async-trait = "0.1"
blueos-api = { path = "libs/api" }
blueos_cli = { path = "libs/adapters/cli" }
blueos_comms = { path = "libs/adapters/comms" }
blueos_cqrs = { path = "libs/logic/cqrs" }
blueos-idl = { path = "libs/idl" }
blueos_jobs = { path = "libs/logic/jobs" }
blueos_logging = { path = "libs/adapters/logging" }
blueos_service = { path = "libs/app/service" }
blueos_settings = { path = "libs/adapters/settings" }
bytes = "1"
clap = { version = "4", features = ["derive"] }
futures = "0.3"
json5 = "0.4"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_arrays = { version = "0.2", default-features = false }
serde_json = "1"
thiserror = { version = "2", default-features = false }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
tracing = "0.1"
tracing-log = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
zenoh = { version = "=1.9.0", default-features = false, features = ["shared-memory", "transport_tcp"] }
