[package]
name = "blueos_comms_channel"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
blueos_comms_driver = { path = "../driver" }

[dev-dependencies]
blueos_comms = { path = "..", default-features = false }
